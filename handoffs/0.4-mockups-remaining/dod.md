# 0.4 — Definition of Done

> Filled by the BUILDER. This table IS the session report.


| Item | Status | Evidence |
|---|---|---|
| Pre-flight rows 1–7, esp. 5 and 6 | PASS | Row 1: `git ls-tree -r --name-only origin/main -- mockups/ \| wc -l` -> `8`. Row 2: `grep -c '^\s*--p-' frontend/src/styles/theme.css` -> `38`. Row 3: `git ls-files "frontend/src/styles/fonts/*"` -> five WOFF2 plus `LICENSE.txt`. Row 4: `grep -c "Status badge scale\|Selected state\|Row opens detail" CONTRACTS.md` -> `3`. Row 5 pre-commit: `git diff --cached --name-only` lists only `mockups/*`, `screenshots/0.4/*`, and this `dod.md`. Row 6: `git check-ignore -v screenshots/0.4/x.png` -> `.gitignore:19:!screenshots/0.4/*.png screenshots/0.4/x.png`. Row 6b after repair: `git ls-files "mockups/*.html" \| xargs grep -c "level of detail"` -> `0` for every file. Row 7: `ls mockups/*.html` -> 19 files; opened `mockups/components.html` and `mockups/components-detail.html` before building. |
| Steps 1–6 verified | PASS | Built Dashboard, Strategy, Security, Services, People, Challenges, and Review states: `dashboard.html`, `dashboard-empty.html`, `strategy.html`, `strategy-unlocked.html`, `security.html`, `services.html`, `people.html`, `challenges.html`, `challenges-item.html`, `review.html`, `review-locked.html`. Repaired `components-detail.html` tab copy per §5.5a only. Browser pass opened all 19 tracked mockups from `file://` with HTTP(S) blocked: zero console errors, zero failed requests, zero horizontal overflow. `npm run lint` exited 0. `npm run build` exited 0. |
| I1, I3–I9, I12–I19 | PASS | I1 primitive token grep -> no output; declaration-context hex/rgb/hsl grep -> no output. I3 token declaration grep -> no output. I4 engine vocabulary grep -> no output. I5 checked against §5.4 fixed data, §5.5 strings, and 0.3 shell strings; justified additions are recorded in the next row. I6 output: one `<link rel="stylesheet" href="../frontend/src/styles/theme.css">` per tracked mockup and no `@import`, `src=`, or URL. I7 Riverside/Grocers grep excluding `data-casepack` -> no output. I8 `State:` counts -> `0` for all 19 tracked mockups. I9 non-dashboard score grep -> no output. I12 tracked `frontend/` + `mockups/` Google font grep -> no output. I13 font files -> five WOFF2 plus `LICENSE.txt`. I14 browser computed styles: Complete `rgb(220,252,231)`, Partly done `rgb(239,246,255)`, Needs attention `rgb(254,243,199)`, Not started `rgb(241,245,249)`, one style per label and four distinct. I15 browser check: strategy-unlocked has four `.option.none`, zero selected, disabled `Lock round`; services selected Basic support and Basic integration; challenges-item selected `Fund` and `Strategic priority`. I16 browser/source check: Security, Services, Challenges rows have chevrons, first-cell `rgb(30,64,175)`, and `tr.open:hover`. I17 Review has all three warnings and enabled compact `Lock round` button, 117x38. I18 dashboard order indexes Warehouse 613, Store Operations 900, Finance 1176, worst-first true. I19 Security has two `tr.open.gap` Not started rows and five full open rows. |
| Eleven files, one state each | PASS | The eleven 0.4 files listed in spec §5.6 exist and each has one state. `git ls-files "mockups/*.html" \| xargs grep -cE "State:"` returned `0` for every tracked mockup. |
| Strings not in §5.5 / 0.3 §5.7, justified | PASS | No lorem ipsum. Added visible strings are either fixed data from §5.4, inherited shell copy from 0.3, or structural labels needed by the specified state. Strategy-unlocked option labels use §5.4/§5.5 business terms already present in the packet: `Low-Cost Leadership`, `Customer insight`, `Marketing & sales`, `Service`; no separate unquoted strategy taxonomy was invented. The repeated `Run-rate rises every round it is not managed` strip label is inherited from the approved 0.3 shell. |
| Screenshots ×33 (11 files × 3 viewports) in `screenshots/0.4/` | PASS | Browser screenshots written at 1440, 1280, 1024 for all eleven 0.4 files. `git ls-files screenshots/0.4/ \| wc -l` -> `33`; listed filenames cover each screen-width pair. Spot-checked `dashboard-1024.png`, `services-1024.png`, and `review-1024.png`; no overlap or clipping. |
| `.gitignore` negation added for `screenshots/0.4` | PASS | Already present on current `main`: `git check-ignore -v screenshots/0.4/x.png` reports `.gitignore:19:!screenshots/0.4/*.png screenshots/0.4/x.png`; no new `.gitignore` edit needed in this commit. |
| 0.3's six mockups untouched | PASS | Existing 0.3 mockups are unchanged except the scoped §5.5a repair to `mockups/components-detail.html`. The staged diff for `components-detail.html` changes only the tab panel copy and the two specified button labels; CSS, tab list, and close control are untouched. |
| Nothing touched outside §1 scope | PASS | Pre-commit scope from `git diff --cached --name-only`: eleven new `mockups/*.html`, scoped `mockups/components-detail.html`, `screenshots/0.4/*.png`, and `handoffs/0.4-mockups-remaining/dod.md`. No `theme.css`, `main.jsx`, `DevTokens.jsx`, fonts, dependencies, backend, or Debrief changes. |
| Auth / instance / casepack canaries | PASS | Auth and instance are **N-A**: static `file://` mockups, no state, no auth. Casepack canary: `git ls-files "mockups/*.html" \| xargs grep -niE "riverside\|grocer" \| grep -v data-casepack` -> no output. |

---
