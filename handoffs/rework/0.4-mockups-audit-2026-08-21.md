# Rework — 0.4 Reference Mockups

Date: 2026-08-21 · Independent audit against current `main` (`174e980`)

## Verdict

REWORK REQUIRED. Rendering is clean at all supported viewports, but the reference set
contains two values for the same capital balance. These mockups are acceptance images for
later student screens, so the contradiction must not be inherited.

## Finding `0.4-RA-001` — capital remaining is `$44,000` and `$46,000`

Severity: Data

Current pack evidence:

```text
backend/packs/riverside_grocery/pack.yaml:40   capital_remaining: 46000
backend/packs/riverside_grocery/pack.yaml:105  capital_committed: 174000
backend/packs/riverside_grocery/pack.yaml:107  capital_remaining: 46000
220000 - 174000 = 46000
```

Current mockup evidence:

- `mockups/review.html:3` and `mockups/review-locked.html:3` say `Remaining $46,000`.
- The shell in 16 mockups says `$44,000 remaining` or `$44,000 of $220,000` (for example
  `mockups/dashboard.html:3` and `mockups/components.html:13`).
- `review.html` therefore contradicts itself on the same rendered page.

This is also reproduced by `python3 backend/scripts/harvest_readback.py`, which reports
pack/derived `46000`, mockup `44000`, and two declared conflicts.

### Required rework

Use `$46,000` everywhere the round-3 shell displays this balance. Do not alter the pack to
match the mockups: the pack value is arithmetically derived from the stated available and
committed figures. Re-capture all affected 1024/1280/1440 screenshots and rerun the offline
browser checks.

### Acceptance

- A tracked-file grep finds no `$44,000` in `mockups/`.
- `harvest_readback.py` reports 43/43 matches and zero declared conflicts.
- All 19 mockups remain free of console errors, failed requests, and horizontal overflow at
  1024, 1280, and 1440 pixels.
