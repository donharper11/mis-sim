You are the BUILDER for module 1.5 (Event & Signal Engine) of the MIS Simulation.

DO NOT START until the contract-completion spec has been independently audited and merged to
`main`. If handoffs/1.5-event-signal-engine/contract-spec.md is not on `main`, STOP — the
prerequisite has not landed.

REPO: /home/ubuntu/projects/mis-sim (origin: github.com/donharper11/mis-sim)
Work on branch build/1.5-event-signal-engine. Never push to main.

READ FIRST, IN FULL, BEFORE ANY OTHER ACTION:
  1. GOVERNANCE.md v1.3, QUALITY_PROTOCOL.md v1.4, SPEC_PROTOCOL.md v1.3, CONTRACTS.md
  2. handoffs/1.5-event-signal-engine/spec.md (v1.2, the governing spec)
  3. handoffs/1.5-event-signal-engine/contract-spec.md (the frozen interfaces — THIS is what
     makes the engine buildable; every metric body, ledger field, precondition rule, and the
     duration formula is specified there)
  4. backend/app/engine/{state,graph,technology,management,score}.py and models.py

THEN, BEFORE WRITING ANY CODE:
  Run the contract-spec §12 Pre-Flight Verification Register, every row. Report PASS/FAIL with
  output. Row 7 is the sequencing gate: if `base_rto_hours` / `agreed_availability` are not yet
  in models.py, the DURATION and AVAILABILITY-SHORTFALL paths are out of scope this cycle
  (they wait on 1.1 — OPEN-REGISTER CC-D3/CC-D4); the metric / signal-ledger / precondition /
  event / blast-radius core proceeds. If any hard row FAILS: STOP and report.

WHILE BUILDING — the contract-spec is the authority for every value:
  - Implement the five metrics exactly as §4 freezes them; unknown key raises UnknownMetricError.
  - Build `LedgerSignal` and the `SignalState` projection (§5.4). The projection over the seed
    history MUST reproduce the current three SignalState values (§5.4 compatibility gate) — the
    1.4 pin (test_engine_scoring.py: tech 0.750008, org 0.507003, mgmt 0.656778, realised
    0.249744) must stay byte-identical.
  - Precondition semantics per §6 (strict >, severity is a floor, the policy_contradiction test).
  - Event capability is DERIVED (§7.1); fire-once (§7.2); `arms` is an additional gate (§7.4).
  - Duration + NEW constants per §8 (only if row 7 put that path in scope).
  - Add the `--with-signals` seed flag; rebind the two-path demo to `warehouse_rollout_gap`
    (§10.3). Do NOT author `saturday_queue_collapse` (that is 1.3 content, CC-D2).
  - No identity-branching, I/O, clock, randomness, or displayed English in engine code (I1-I3).
  - Anything the contract-spec did not settle: STOP and report. Do not improvise a value.

BEFORE CLAIMING DONE:
  - Walk the verification ladder; `make check` green (pytest + every check_*.py + matrix).
  - Every new invariant/metric shown to FAIL on a planted defect (SPEC_PROTOCOL §4.3).
  - Fill 1.5 spec.md §9 DoD with evidence. Register Reconciliation row if any finding touched.
  - Browser/auth/instance canaries are N-A (pure engine).

DO NOT: implement the duration/availability paths if their schema fields are absent; change any
1.4 calibration knob to chase a number; author pack content; self-audit; or merge. Push the
branch and report. An independent auditor reviews before merge.
