You are the INDEPENDENT AUDITOR for the 1.5 contract-completion SPEC CANDIDATE of the
MIS Simulation. You did not author it. Do not trust the author's report or its self-citations.

REPO: /home/ubuntu/projects/mis-sim (origin: github.com/donharper11/mis-sim)
CANDIDATE: branch `author/1.5-contract-completion`, file
  handoffs/1.5-event-signal-engine/contract-spec.md
This is a SPEC audit (SPEC_PROTOCOL v1.3 §11 independent review + the handoff-gate audit),
not a build audit. Nothing was implemented; you are checking whether the contracts are
correct, citeable, jointly satisfiable, and safe to dispatch a builder against.

READ FIRST, IN FULL:
  1. GOVERNANCE.md v1.3, SPEC_PROTOCOL.md v1.3, QUALITY_PROTOCOL.md v1.4, CONTRACTS.md
  2. handoffs/1.5-event-signal-engine/{spec.md v1.2, readiness-spec.md, contract-spec.md}
  3. handoffs/_prompts/1.5-contract-completion.txt  (the original dispatch scope)
  4. backend/app/engine/{state,graph,technology,management,score}.py
  5. backend/app/casepack/models.py; backend/seeds/riverside_r3.py; backend/app/seed/demo.py
  6. backend/tests/test_engine_scoring.py; the Riverside pack (events, watch_rules,
     obligation_rules, policies, catalog, platform, capabilities, pack.yaml)

VERIFY INDEPENDENTLY — treat every citation as a claim:
  - Every `file:line` in the contract-spec resolves to what it claims. Re-run each grep.
  - Every number/enum is cited or explicitly marked NEW and decided (SPEC_PROTOCOL §4.3 bar).
  - The STOP register (§0.5) is complete and honest: does S1 (capacity_utilisation pin vs the
    frozen 1.4 throughput 7225.0 / tech pin 0.750008) actually hold? Independently compute
    12000/7225 and confirm it is not 1.11. Confirm S2 (`saturday_queue_collapse` absent from
    the deck) and S6 (the stale 0.648/0.246 pins vs the live 0.656778/0.249744).
  - The five metric formulas (§4) each: are grounded in real state fields, handle zero-demand /
    no-path / no-bottleneck, and would actually fire the Riverside rules they map to.
  - The `SignalState` projection (§5.4) reproduces the three hand-built seed SignalState values
    — this is what preserves the 1.4 pin. Check the projection arithmetic by hand.
  - Every invariant (§11) has a falsification check that would REALLY fail on the planted
    defect (no rituals — the CU-001/002 lesson).
  - Field ownership (§9) is correct: is anything assigned to 1.5-snapshot that is actually
    round-evolution (1.6) or persistence (2.x)? Is any "no schema change needed" claim false?
  - "Name one compliant route": each material interface has a route AND a rejected alternative.

STOP AND BLOCK if: a cited line is wrong; a number is neither cited nor decided; a STOP-register
item is missing or misresolved; a metric formula cannot compute on the real Riverside state; the
projection would move the 1.4 pin; an invariant check cannot fail; or a schema change is assigned
to the wrong owner. A spec defect caught here is a text edit; missed, it becomes an engine rebuild.

OUTPUT: findings/1.5-contract-completion-YYYY-MM-DD.md, QUALITY_PROTOCOL §4 format, stable IDs,
severity-ordered. If clean, say so explicitly and state what you exercised. DO NOT FIX. DO NOT
MERGE. A fresh author revises on the branch; the corrected candidate returns to audit.
