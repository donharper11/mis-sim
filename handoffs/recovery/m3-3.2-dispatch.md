# M3 packet 3.2 dispatch — authenticated shell

Date: 2026-09-16  
Base: `b65f7dd`  
Depends on: accepted M2 auth, instance scoping, registry, scheduling, and M3.1 components.

## Delivered boundary

The shell owns the authenticated `/` route, sidebar and decision grouping, top bar, current
round context, capital/status strip, and current schedule deadline. It reads `/api/auth/me`,
`/api/instances/{instance_id}`, and the instance-scoped schedule endpoint. It renders honest
loading, missing-context, and error states; it does not invent capital, score, estate, or
decision values. Future packets replace the placeholder body with domain screens.

Allowed implementation paths for this packet were:

```text
backend/app/api/platform.py
frontend/src/App.jsx
frontend/src/components/AppShell.jsx
frontend/src/pages/Shell.jsx
frontend/src/styles/theme.css
docs/frontend-shell.md
handoffs/recovery/m3-3.2-dod.md
```

No simulation mutation, score computation, auth contract, scheduling service, mockup, font,
dependency, or domain screen is part of 3.2.
