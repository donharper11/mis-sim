# M3.3 Dashboard DoD

| Check | Result |
|---|---|
| Read route is instance-authorized | PASS — route depends on `get_current_instance` |
| Student team isolation | PASS — response filters to active student enrollment team; API test covers it |
| Scorecard source | PASS — latest persisted `RoundResult.payload.scorecard`; no engine call |
| Signal source | PASS — payload open bucket with persisted `signal` fallback |
| Unit-response source | PASS — persisted deployment, architecture and org-unit rows; checkpoint fallback |
| Missing runtime data | PASS — nullable values and explicit empty states |
| Frontend lint | PASS — `npm run lint` |
| Frontend build | PASS — `npm run build` |
| Backend focused tests | PASS — dashboard API plus auth/guard/platform suite, 15 tests |
| Browser probe | PASS — 1024px mocked API: Dashboard heading, scorecard, attention item, no overflow/page error |

The packet deliberately leaves decision screens, score recalculation, event response and the
debrief for their own packets. Dashboard navigation remains presentational until those routes
are implemented.
