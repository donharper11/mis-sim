# Findings — 1.4 Closeout (information-policy scoring dimension)
Date: 2026-08-21 · Auditor: Claude (opus-4-8), fresh context, detached worktree
Build: `build/1.4-closeout` tip `6143250` (cut from `main` at `34238f4`)
Spec: `handoffs/1.4-scoring-engine/closeout-spec.md` · Builder report: `handoffs/1.4-scoring-engine/closeout.md`

## Verdict

**PASS WITH FINDINGS.** Mergeable on the engineering merits — no Blocking or Functional
findings; all findings are Report/Data-level documentation/test-hygiene nits. The auditor
does not authorise the merge; the builder does not declare approval. A merge is a separate
human/process decision.

Everything in this file was exercised independently against a detached checkout of `6143250`
and cross-checked against base `34238f4`. Nothing was fixed, merged, or pushed.

---

## Blocking

- none.

## Functional

- none. The code implements the frozen spec correctly. Formula, aggregation, null path,
  discipline floor, state contract, and all negative cases behave as specified.

## Report

- 1.4C-R01 (from builder deviation D2): `backend/tests/check_policy_options.py` §6 last
  check — "model/contract describe the 1.4 policy-distance consumer as PROSPECTIVE, not
  current" (check_policy_options.py:242-247) — is now **semantically stale**. The closeout
  lands that consumer (`management.policy_switch_alignment`, live), so the assertion's intent
  is false. It passes only mechanically: the exact banned strings ("reads ordinal distance")
  were avoided (CONTRACTS.md now says "consumes the ordinal distance"), and the required
  keyword "deferred" still appears — but for the *producer* (1.6/2.x), a different subject.
  Builder disclosed this (D2) and recommended re-pointing to "consumer live, producer
  deferred". Disposition: ACCEPT builder's disposition; a fast follow-up should re-point the
  guard. Non-blocking: the real contract surfaces (models.py, CONTRACTS.md, docs) are all
  correct; only the guard's stated purpose drifted.

- 1.4C-R02: builder report over-counts the new test file. `closeout.md` §1 and `dod.md`
  Step-1 row both say "24 tests" for `tests/test_policy_dimension.py`; actual pytest items =
  **18** (`grep -c "def test_"` = 18; `pytest --collect-only` = 18 collected). All 18 pass;
  coverage (C2–C7, aggregation/null/discipline, C11/C12, six negatives) is genuine. Report
  count inaccuracy only.

## Data

- 1.4C-R03: `handoffs/1.4-scoring-engine/spec.md` §5.6 (line 223) — the illustrative
  decomposition-record example still shows the pre-closeout `"mgmt": 0.648` / `"realised":
  0.249` and omits the two new policy sub-factors. The authoritative seed pin in §5.7
  (line 243) is correctly updated to `Mgmt 0.656778 · realised 0.249744`. Minor doc
  staleness in an illustrative shape snippet; not load-bearing. Non-blocking.

---

## Builder deviation dispositions (closeout.md §5)

- **D1** (models.py `PolicyOption.options` docstring corrected, outside the §5.5 five-doc
  list): **ACCEPT.** Docstring-only (`git diff` confirms no code change); the ordinal-contract
  tokens ("ordinal", "least constrained") are preserved so `check_policy_options.py` stays
  green; the correction was necessary because replacing the deferred hook made the old
  "today it raises NotImplementedError" clause factually false. Correctly flagged, not silent.

- **D2** (check_policy_options §6 stale): **ACCEPT with finding 1.4C-R01** (re-point
  recommended). Non-blocking.

- **D3** ("unknown stakeholder archetype" implemented as exclusion, not a raise):
  **ACCEPT.** This is a genuine spec-internal contradiction: decision 3 and the §5.3 table
  make an archetype with no policy row an *exclusion* (denominator omission), while decision 9
  lists "unknown stakeholder archetypes" as a `ValueError`. There is no archetype-registry
  model, so "unknown archetype" is indistinguishable from "archetype with no policy row".
  Per decision 4 ("STOP rather than invent a second resolver"), the builder implemented the
  exclusion and did not fabricate a registry. Verified: setting a stakeholder's archetype to a
  non-existent key excludes it (36 → 31 rows, no raise). Defensible and disclosed; I note the
  decision 3 vs decision 9 tension as a spec ambiguity for the author, not a build defect.

- **D4** (`overrides` for the policies domain not consumed): **ACCEPT.** Riverside's
  `overrides` is `[]`; the field is `list[dict[str, Any]]` with no per-domain schema. Decision
  4 forbids inventing a second resolver. No behaviour change for any current pack. Disclosed.

---

## What I exercised (independent verification)

**Battery (spec §10), from `backend/`:**
- `python3 -m pytest -q` → **32 passed**.
- `PYTHONPATH=. python3 tests/check_fixture_matrix.py` → all 42 fixtures behave as named.
- `PYTHONPATH=. bin/validate_casepack packs/riverside_grocery` → `0 errors · 0 warnings ·
  exit 0`; `--json` → `[]`, exit 0.
- `PYTHONPATH=. python3 -m app.seed.demo --scenario riverside_r3` → prints "policy decisions
  6 (6 actively decided)".
- `PYTHONPATH=. python3 -m app.engine.score riverside_r3` → order_fulfilment tech 0.750 org
  0.507 mgmt 0.657 realised 0.250 throttle org.
- `PYTHONPATH=. python3 tests/check_policy_options.py` → 13/13 (last one stale — 1.4C-R01).

**Pins reproduced (order_fulfilment), computed from the seed, not asserted:**
- Tech **0.750008**, Org **0.507003** — byte-identical to base `34238f4` (invariant C1). ✓
- Mgmt **0.656778**, realised **0.249744**, throttle **org**, firm_score **0.254585**. ✓
- `policy_alignment` **0.4676** (36 preference rows, total weight 19.29), `policy_discipline`
  **1.0**. ✓
- Independently recomputed geomean of the eight mgmt sub-factors = 0.656778; geomean of the
  original six = 0.648006 (= builder's stated baseline); `tech·org·mgmt` = 0.249744. ✓

**Asymmetric ordinal formula — hand-reconstructed and matched to code**
(`_asymmetric_alignment`): permissive miss pays full normalised distance, strict overshoot
pays half.
- n=4, span 3: permissive 3-step (t0,i3) = 0.0; strict 3-step (t3,i0) = 0.5 → strict costs
  exactly half. One-step: permissive 0.6667 vs strict 0.8333 (penalty 0.3333 vs 0.1667).
- Two-option opposite ends (n=2): permissive 0.0 / strict 0.5. Matches §5.3 exactly.

**Invariants:**
- C1 — Tech/Org unchanged to 1e-6: confirmed by direct base-vs-build comparison. ✓
- C8 — the six pre-existing mgmt sub-factors byte-identical at base and build (governance 1.0,
  strategic_alignment 0.937408, portfolio_discipline 0.811935, signal_responsiveness 0.333333,
  follow_through 0.8, stakeholder_alignment 0.3648); only two keys added. ✓
- C9 / I1 — no casepack-identity branch in `app/engine`; all grep hits are in tests/fixtures
  (the excluded category). ✓
- C10 / I2 — engine pure: purity greps clean; the only "yaml" hit is a docstring word in
  `__init__.py`, not I/O. ✓
- C11 — mutating one team selection (0.4676 → 0.5279) and, independently, one preference ideal
  (0.4676 → 0.4536) each moves the score. ✓
- C12 — empty `policy_decisions` → discipline 0.25, mgmt 0.656778 → 0.552282 (lower). ✓
- C6 — active vs inactive default: alignment equal (0.4676), discipline higher (1.0 vs 0.25). ✓
- C13 — raw-fit isolation guard 2/2 pass. ✓
- I5 determinism (100× same hash), I6 plain product (`tech * org * mgmt`) present. ✓

**Negative cases (§5.3 / C7) — all raise `ValueError`, exercised directly:**
duplicate runtime decision; unknown policy; selected outside options; missing default for the
null path; preference ideal outside options. ✓ (five of five)

**Scope containment:**
- `backend/packs/**` and `backend/harvest/**` untouched → no casepack value/weight/formula
  tuning to recover the old 0.648 (decision 11). `harvested_raw_fit` consumption unchanged
  (not present in the diff).
- `score.py`, `technology.py`, `organisation.py`, `rollup.py` untouched → Tech/Org/BSC/graph
  formulas unchanged.
- `frontend/**`, `backend/app/api/**` untouched → no browser-visible surface changed; the
  code-inspection-only (N-A) claim for browser/UX rungs is proven by the diff scope.
- `main` remains at `34238f4` (unchanged). Preserved auditor files (findings/1.1|1.2|1.3-*)
  are not in the diff.

**Document deltas (§5.5) — all five landed and accurate:**
spec.md (§5.3a formula/table, §5.7 pin + history, changelog); dod.md (Closeout section
appended, historical evidence intact); design/02 (data-freshness row → deferred with owners
3.4/1.6, strategic_alignment "dot product" → "cosine similarity", two new policy rows);
OPEN-REGISTER (F4 + E2 CLOSED, G2 named deferral to 3.4 + 1.6); CONTRACTS.md (new
`PolicyDecisionState` runtime-snapshot contract with producer 1.6/2.x + scorer consumer,
`PolicyOption.options` consumer moved prospective → live, header date). G2 is a NAMED
deferral; F4/E2 are closed by working, tested code; strategic_alignment reads "cosine
similarity".

**Seed content:** all six seed decisions sit at their authored `default` with
`actively_decided=True` (decision 10); no casepack content invented.
