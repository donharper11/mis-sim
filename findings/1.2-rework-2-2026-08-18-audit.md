# Findings — 1.2 Casepack Validator · SECOND REWORK

**Date:** 2026-08-18 · **Auditor:** Claude (fresh context) · **Branch:** `build/1.2-rework-2` @ `1d1444f`
**Rework instruction:** `handoffs/1.2-validator/rework-2.md` · **Spec:** `handoffs/1.2-validator/spec.md` v1.2
**Base:** `main` @ `dfa3bc3`, which is also `git merge-base main build/1.2-rework-2`
**Measured against:** `handoffs/1.5-event-signal-engine/spec.md` v1.1 §3 decision 10 · §5.1a · §5.2a · §10 ·
`handoffs/1.3-harvest/spec.md` v1.1 §3a · §6 · `findings/1.1-rework-2-2026-08-17-audit.md` ·
`GOVERNANCE.md` §4.9 · §4.10 · §5 · §6 · `QUALITY_PROTOCOL.md` §2 · §4 · `SPEC_PROTOCOL.md` §2.1 · §3 · §4.1

> **Independence: fit, no caveat.** I did not author `rework-2.md`, did not author the 1.2, 1.3 or 1.5
> specs it is measured against, and did not build this branch. `GOVERNANCE §6.1` puts casepack schema
> and `E*` codes in the "fresh agent, mandatory" row. The module is a headless CLI:
> `QUALITY_PROTOCOL §2` rungs 3–5 (browser, console, navigation) are **N-A and declared** — there is
> no runtime, no route and no screen in this packet. Rung 3's seed obligation is met by the fixture
> packs under `backend/tests/fixtures/packs/`, every one of which was re-run below.

**Method.** `dod-rework-2.md` was read **after** the governing-rule diff, the prohibited diffs, the
whole-population output comparison, the fixture matrix on both trees, the four paired-fixture pairs
and all six items had already been constructed, run and recorded. Nothing here is a re-run of pasted
output taken as agreement. Every behavioural claim was reproduced by **building the shape myself**,
not by reading the code: the decision-10 edge was tested by authoring an event that waits for
`warning` from a presence rule, and then by **mutating `_raisable_severities` to over-widen and
confirming the suite catches it**. The compatibility rule was tested by running both validators over
**all 25 packs** and classifying every differing line, not by comparing one `Counter`.

The branch and the pre-rework base were checked out into **git worktrees under the scratchpad**, so
the shared working directory stayed on `main` @ `dfa3bc3` and its `HEAD` never moved. Every probe
pack was built under the scratchpad, never under `backend/packs/`. The only file this audit adds to
the repository is this one.

---

## Blocking

**None.** The governing rule holds across the whole pack population, the prohibited diffs are empty
from both candidate baselines, the fixture matrix exits 0 on the branch and **exits 1 on the
pre-rework build**, all six items do what they claim when the shapes are constructed by hand, and the
experiment this packet exists for reproduces exactly: `E12 ×2` and `E21 ×2` vanish and `E20` falls
6 → 5.

`1.5` decision 10 — the one item most likely to be got wrong — is implemented correctly **and is
guarded by a fixture that fails when it is loosened.** That was verified by mutation, below.

The findings are about message copy that now points the wrong way, and about two defects in the
**instruction** rather than the build.

---

## Functional

### 1.2-030 — `E21`'s `Fix:` line still tells the author to set a threshold on a presence rule, which is the one shape `models.py` refuses to load. The reason forked; the fix did not — BUILD

Item 3.3 forked `E21`'s **reason** so a presence rule gets its own wording
(`validate_messages.yaml:63`, `severity_unreachable_presence`), and `dod-rework-2.md` §2.3 states
the motivation in so many words: *"The existing reason tells an author to set the missing threshold;
on a presence rule that is an instruction to author a shape `models.py` rejects."*

The `fix` string is shared by both arms and was not forked (`validate_messages.yaml:181-185`):

```
  E21:
    severity: ERROR
    subject: "Event {event}"
    message: "Can never happen: {reason}."
    fix: "Fix: change what the event waits for in events.yaml, or set the matching threshold in watch_rules.yaml."
```

Constructed — `minimal_valid` with `rec_adopt_01` made `metric_kind: presence`, plus one event
waiting on it at `warning`, run on this branch:

```
  ERROR  E21  Event audit_probe               events.yaml:100
         Can never happen: it waits for 'rec_adopt_01' to reach warning, and that rule watches for
         a condition simply being present, which is always raised as critical and never as a warning.
         Fix: change what the event waits for in events.yaml, or set the matching threshold in watch_rules.yaml.
```

The reason says *there is no threshold here*; the next line says *set the threshold*. Following it
costs the author the whole pack — the same probe with `critical_above: 0.9` added to that rule:

```
  ERROR  E00  Unreadable pack                 watch_rules.yaml
         This pack could not be read: watch_rules.yaml: watch_rules.1: Value error,
         metric_kind 'presence' must not carry warn_above or critical_above
```

Spec §3 decision 3 — *"every check names its fix … a message that states a problem without a next
action is incomplete"* — is not satisfied by a next action that is invalid. `GOVERNANCE §4.10` is the
same rule pointed at users. This is **undeclared**: `dod-rework-2.md` §8 lists seven substitutions
and this is not among them, while its sibling (`1.2-031`) is.

Not blocking: no exit code, no invariant and no finding count moves. It is Functional because it is
wrong copy in a message this packet's own item 3.3 rewrote, on the code path this packet created.

**Closes when** `E21` gains a `fix` variant for the presence arm — the mechanism already exists in
the same file (`variants:`), and the correct wording is the reason's own: *change what the event
waits for, or change the rule's `metric_kind`*.

### 1.2-031 — `E02` and `E23` still tell the author that only a catalog item can hold an entity, after item 3.4 made a platform service a legal holder. The `Fix:` half was declared; the `message:` half was not — BUILD

`validate_messages.yaml:93-98` and `:193-198`, unchanged by this branch:

```
  E02:
    message: "Needs {entity} information at '{level}' detail, and no catalog item can hold it."
    fix: "Fix: add a catalog item whose owns_entities holds {entity} at '{level}' or finer, or lower min_level_of_detail in capabilities.yaml."
  E23:
    message: "Needs {entity} information at '{level}' detail, and nothing this company can buy produces it."
    fix: "Fix: add a catalog item owning {entity} at '{level}' or finer, or lower the requirement in questions.yaml."
```

After item 3.4 the second half of each is now a legal and — for Riverside — the **intended** remedy.
Constructed on a scratch Riverside outside `backend/packs/`, adding exactly what 1.3 §3a addition 4
tells 1.3 to add:

```yaml
  - key: central_sign_on
    roles_filled: [identity_access]
    owns_entities: [{entity: user_account, level_of_detail: named_user}]
```

```
branch @ 1d1444f   E07 ×8 · E14 ×1 · E20 ×5            14 errors · 7 warnings · exit 1
pre    @ dfa3bc3   E02 ×1 · E07 ×8 · E12 ×2 · E14 ×1 · E20 ×6 · E21 ×2   20 errors
```

`E02` clears on the branch and does not clear on the base — the change works. But the message a 1.3
author reads before making that edit still says *"no catalog item can hold it"* and *"add a catalog
item"*, pointing away from the one file the fix actually lives in. `E01`'s fix already carries the
two-place wording for roles, so there is a precedent in the same catalogue and no design to do.

`dod-rework-2.md` §8 item 6 declares the `Fix:` half and rules it *"not wrong, only incomplete"*.
**Ruling: the `Fix:` half is under-described and the declaration is fair; the `message:` half is a
statement of fact that is now false**, and it was not declared. `E02`'s message asserts that no
catalog item can hold the entity — true — and offers that as the reason the requirement is
unsatisfiable, which after 3.4 it no longer is on its own.

Not blocking. It is Functional because `E02 ×1` is the error 1.3 §3a singles out, and this is the
first sentence the 1.3 author will read about it.

**Closes when** both codes' `message` and `fix` name `platform.yaml`'s services alongside
`catalog.yaml`, in `E01`'s existing phrasing.

---

## Data

### 1.2-032 — `rework-2.md` §5 authorised "exactly one line" in `spec.md` §5.3. There is no compliant route with one line: `I1` reads the **header** code-list line and hard-exits on a §5.3 code that is outside it — REWORK INSTRUCTION

The instruction (`rework-2.md` §5, the `W08` and the spec box):

> *"You are authorised to add exactly that one line to §5.3, and nothing else in the spec"*

`check_fixture_matrix.py:115-143` — unchanged by this branch, so this was true before the builder
was dispatched — makes the **header** the authority and then treats §5.1–5.3 as a consistency check
against it:

```python
header = next(line for line in text.splitlines() if "Code list is versioned" in line)
...
stray = listed - named
if stray:
    raise SystemExit(f"spec.md disagrees with itself: {sorted(stray)} listed but not in the header range")
```

Constructed. The branch's tree with the header reverted to `W01`–`W07` and `W08` left in §5.3 — that
is, the instruction executed literally:

```
$ python3 tests/check_fixture_matrix.py
...
spec.md disagrees with itself: ['W08'] listed but not in the header range
EXIT=1
```

And the converse, header changed with §5.3 left alone:

```
I1  implemented codes : 29   spec-named codes : 29   set equality : PASS
EXIT=0
```

So the single load-bearing line is the **header**, not §5.3, and the instruction named the wrong one.
Executed as written the packet would have failed its own re-run artifact; the minimum
self-consistent edit is two lines. **The builder took two, declared it in `dod-rework-2.md` §7 with
the mechanism quoted correctly, and changed nothing else in the spec** — verified line by line:

```
$ git diff dfa3bc3..1d1444f -- handoffs/1.2-validator/spec.md
-**Code list is versioned … `W01`–`W07` · `I3` · `I8`
+**Code list is versioned … `W01`–`W08` · `I3` · `I8`
+W08  a strategy fewer than 6 event cards can be dealt to — the per-strategy draw check
+     of 1.5 §5.2a at its O4 default of N = 6                              NEW rework-2
```

**Ruling: the substitution is accurate, sound and forced. The finding is against `rework-2.md`,
not the build.** `SPEC_PROTOCOL §4.1` is the rule it breaks — an authorisation is an invariant set,
and this one had no compliant route. It is the same failure class as module 0.3 v1, in a smaller
register.

**Closes when** the instruction's spec-edit clause names both lines, or names the header as the
authority and §5.3 as the reader-facing consequence.

### 1.2-033 — the versioned code list changed without a version bump or a changelog entry, so `spec.md` now presents a code the amendment it is dated to never named — REWORK INSTRUCTION · SPEC

`SPEC_PROTOCOL §3`: *"Interface freezes are explicit … casepack schema keys: FROZEN or VERSIONED.
Silent changes forbidden."* `GOVERNANCE §8`: *"Contract changes are merged into the living document
(version bump + changelog entry), never shipped as standalone delta files."*

On the branch, `handoffs/1.2-validator/spec.md:3-5`:

```
**Authored under** `SPEC_PROTOCOL.md` v1.1 · **Author:** Claude · **Date:** 2026-07-26
**Spec version:** v1.2 · **Amended:** 2026-08-14, post-audit, against `findings/1.2-2026-08-14-audit.md`
**Code list is versioned, not frozen** … `W01`–`W08` · `I3` · `I8`
```

The header is stamped **v1.2, amended 2026-08-14** and the §10 changelog's v1.2 table has no `W08`
row — while the versioned list it sits beside now carries `W08`, added 2026-08-18 by a different
document's authority. A reader of `spec.md` cannot tell that the list moved, and `1.5 §10 item 3`
(where `W08` was actually ruled in) is not cited anywhere in the spec.

The builder did exactly as instructed — `dod-rework-2.md` §7: *"Nothing else in `spec.md` changed —
no changelog entry, no DoD row, no §9.1 edit."* The instruction's *"and nothing else in the spec"*
is what forbade the bookkeeping that `GOVERNANCE §8` requires.

Not blocking, and not the builder's to have fixed unasked (`GOVERNANCE §7`, `QUALITY_PROTOCOL §6`).
Recorded so the spec is repaired by its author rather than drifting.

**Closes when** `spec.md` bumps to v1.3 with a one-row changelog entry citing `1.5 §10 item 3` and
this rework instruction, or the author states that a code-list extension is exempt from §8.

---

## Report

### 1.2-034 — the in-code justification for `W08`'s flat `N` is wrong. Keeping it flat was right; `I4` is not the reason, and `W05` two hundred lines away already derives from `pack.rounds` — BUILD (report)

`validate.py:40-44`:

```python
#: W08 threshold -- 1.5 spec section 5.2a, at N = 6 as ruled by its open decision O4
#: ("6, one per round"; the packs it was ruled against play six rounds). It is a flat
#: constant, NOT a function of pack.rounds: O4 fixes the number, and naming a pack here to
#: derive it would breach invariant I4 as surely as branching on one would.
W08_MIN_DRAWS = 6
```

`I4` is *"No pack-identity branching"* — `grep -rniE "riverside|grocer" backend/app/casepack/validate*`
→ zero, which this branch holds. Reading a **declared field** of whatever pack is loaded is not
pack-identity branching, and the module already does it for the neighbouring check:

```
$ grep -n "metadata.rounds" backend/app/casepack/validate.py
564, 1148, 1390
validate.py:1147  """W05 -- fewer cards than rounds, so some round has nothing to deal."""
validate.py:1148      rounds = lens.pack.metadata.rounds
```

If deriving `N` from `pack.rounds` breached `I4`, `W05` would breach it today. The **decision** is
right for a different reason, which `dod-rework-2.md` §8 item 4 states correctly — 1.5's `O4` fixes
the number, and re-deriving it is an author's ruling on another packet's open decision, not a
builder's call (`GOVERNANCE §4.4`). Only the code comment argues it from the wrong premise, and a
comment is what the next builder reads.

**Closes when** the comment cites `O4` and `GOVERNANCE §4.4` rather than `I4`.

### 1.2-035 — `W08` and `W03` pull in opposite directions: the empty-affinity card `1.5 §5.2a` sanctions as every strategy's draw is exactly the card `W03` warns about — 1.5 · 1.3

`1.5 §5.2a` defines the count as *"events whose `strategy_affinity` includes S, **or is empty**"*,
and the branch implements that faithfully (`validate.py`, `check_strategy_draws`:
`if not event.strategy_affinity or strategy.key in event.strategy_affinity`). `W03` warns on every
event with no `strategy_affinity`. So the cheapest documented route to closing `W08` is the one the
adjacent warning objects to. Observed as `1.1-r2-012` before `W08` existed; now both checks are in
the same catalogue and both fire on the same pack.

Constructed — `broken_W08` (seven cards, one strategy) with every affinity emptied, on this branch:

```
before   WARN W08 ×1   (continuity_of_care draws 0)          0 errors · 1 warning · exit 0
after    WARN W03 ×7   (every card targets no strategy)      0 errors · 7 warnings · exit 0
```

`W08` closes and the warning count goes up sevenfold. Neither blocks — both are `WARN` — but 1.3 is
the packet that meets this, `CG-2` is on its DoD as `I9`, and *"add universal cards"* is the obvious
move. Recorded so 1.3 is told which of the two the author intends to win rather than discovering the
trade at step 6.

**Closes when** an author rules on whether an empty `strategy_affinity` is a legitimate authored
shape (in which case `W03` needs a carve-out) or a smell (in which case `1.5 §5.2a`'s *"or is
empty"* needs one).

### 1.2-036 — twenty pre-existing `broken_*` fixtures now emit an unasserted `W08`, so 22 of 29 matrix rows would not notice a `W08` regression — BUILD (report)

Declared as `dod-rework-2.md` §8 item 7, and the reasoning given there is sound: the fixtures carry
the four-card deck they were derived from, rewriting twenty packs is diff and risk for no coverage,
and incidental extra findings are already the suite's norm. Verified across the whole population —
running both validators over the 25 packs that exist on the base and classifying every differing
line:

```
packs compared: 25 · identical: 3 · differ by W08 blocks and the summary line only: 22 · other diffs: 0
```

Recorded, not disputed, for one reason: `W08` now appears in 22 of 29 matrix rows as an unasserted
by-product, and no row forbids it except `broken_W08`'s sibling assertions. A future change that
made `W08` fire wrongly would show up in exactly one place. The suite is still the strongest it has
been on this module; this is where its next weak spot is.

**Closes when** the matrix either forbids `W08` on the fixtures whose purpose has nothing to do with
the deck, or the fixtures' decks are extended when someone is in them for another reason.

### 1.2-037 — 1.3's `I11` still has no executable check. It is not one of this packet's six items, and it is the one reason 1.3's gate does not lift completely — 1.3 · 1.5 (carried from `1.1-r2-005`)

1.3 §6 `I11`: *"`obligation_rules.yaml` exists and references real entities and policies … zero
orphans."* Re-derived on this branch rather than carried from the prior audit — a scratch pack whose
obligation section is entirely orphaned, built under the scratchpad from `minimal_valid`:

```
$ printf -- '- key: nonsense\n' > <scratch>/obligation_rules.yaml
  ERROR  E00  ... (missing required fields — the model, not a cross-reference check)
```

and a **well-formed** obligation naming an entity, a policy, a `cleared_by` action and an `arms`
event that none exist produces no finding naming the section at all. `grep -o '\.label("[a-z_]*"'`
aside, there is no `obligation` string anywhere in `validate.py`:

```
$ grep -n "obligation" backend/app/casepack/validate.py
(no hits)
```

This is correct behaviour for this branch — obligation cross-referencing is not among
`rework-2.md`'s six items and is on no handoff list (`1.1-r2-005`, item 6 of that audit's Part D2).
Recorded here because `E2`'s answer below turns on it: `I6`, `I8` and `I9` all become reachable with
this merge and `I11` does not.

**Closes when** `1.5 §10`'s to-1.2 list gains obligation cross-reference checks (`entity` →
`entities.yaml`, `policy` → `policies.yaml`, `cleared_by` → `ACTION_TYPES`, `arms` → event keys) and
a 1.2 packet builds them.

### 1.2-038 — `minimal_valid` gained three event cards to clear `W08`. Ruled SOUND: the deck was thin on its own terms, not only against `N = 6` — BUILD (report)

Declared as `dod-rework-2.md` §8 item 3. The concern is real enough to test: a fixture edited to
make a new check silent is a fixture bent to fit, and `GOVERNANCE §4.9`'s stub/seed line is exactly
where that shows.

**On content — passes.** The three cards (`out_of_hours_overflow`, `duplicate_client_records`,
`weekend_phone_bookings`) each carry a reachable precondition, both strategies in the affinity, three
priced options with tags, a scorecard delta, a `provenance` note and a real prose label in
`labels.yaml` — *"The Kelly household is in the system four times, once for every practice they have
ever walked into."* No `TODO`, no placeholder, no repeated tuple. They are the same practice this
fixture has always described. `diff -rq` against `minimal_valid` shows every other new fixture is a
3-to-24-line derivation of it, which is `rework-2.md` §4's *"real, coherent, minimally-different"*.

**On the accusation — does not stand, and this is the part that had to be constructed.** If `N = 6`
were the only reason, then at `N = pack.rounds` the untouched four-card deck would have been clean.
It is not. `minimal_valid` plays **four** rounds, and its four cards gave `low_cost_care` 3 draws and
`continuity_of_care` 3:

```
$ (branch tree, W08_MIN_DRAWS forced to 4 = this pack's own rounds)
$ ./bin/validate_casepack tests/fixtures/packs/minimal_valid      # the OLD four-card deck
  WARN   W08  Continuity of Care     …
  WARN   W08  Low-Cost Care          …
  0 errors · 2 warnings · exit 0
```

The deck was under-authored against **any** reading of `O4`, so the cards were owed regardless.

**One coupling stays open.** The pack now carries seven cards for four rounds, which only `N = 6`
requires; at `N = pack.rounds` it would have needed five. That is `1.5 O4`'s to settle (`D3` below),
and if it moves, three of these cards become surplus rather than wrong. Recorded so the row is
revisited with the ruling rather than assumed settled by it.

---

# PART A — the governing rule

**Verdict: it holds, and it holds across the whole pack population rather than on Riverside alone.**

## A1 · Riverside's error profile, derived then measured

Derived before running anything, from `spec.md` §9.1's gate and the pack itself: Riverside declares
no `metric_kind` on any rule and no `owns_entities` on any platform service, so under the
`threshold` default every rule stays illegal, every capability stays as covered as it was, and
`Lens.owned` gains nothing. **Every error must therefore be identical, and only `W08` may appear.**

Both trees, same pack, full text output:

```
$ (wt-pre    @ dfa3bc3)  ./bin/validate_casepack packs/riverside_grocery   exit 1
$ (wt-branch @ 1d1444f)  ./bin/validate_casepack packs/riverside_grocery   exit 1

$ diff pre-riverside.txt br-riverside.txt
123c123,143
<   20 errors · 3 warnings · exit 1
---
>   WARN   W08  Cost Leadership                 events.yaml   (only 3, needs 6)
>   WARN   W08  Customer and Supplier Intimacy  events.yaml   (only 2, needs 6)
>   WARN   W08  Differentiation                 events.yaml   (only 1, needs 6)
>   WARN   W08  Focus Strategy                  events.yaml   (only 1, needs 6)
>   20 errors · 7 warnings · exit 1
```

**The entire diff of the two runs is the four `W08` blocks and the summary line.** Not the counts —
the bytes. Every `ERROR` line, locator, field path, `Fix:` line and ordering is unchanged.

```
BEFORE  E07 ×8 · E20 ×6 · E12 ×2 · E21 ×2 · E02 ×1 · E14 ×1 · W02 · W04 · W05         20 errors · 3 warnings
AFTER   E07 ×8 · E20 ×6 · E12 ×2 · E21 ×2 · E02 ×1 · E14 ×1 · W02 · W04 · W05 · W08 ×4  20 errors · 7 warnings
```

The four `W08` counts are **3 / 2 / 1 / 1** on `cost_leadership` / `customer_supplier_intimacy` /
`differentiation` / `focus_strategy` — exactly the numbers `1.5 §5.2a` computed independently from
`events.yaml` and `rework-2.md` §3.5 predicted. Exit code unchanged at 1, which is `O1`'s *"WARN
never blocks"* holding.

**Widened beyond the instruction.** A `Counter` on one pack is a weak instrument. Both validators
were run over **all 25 packs** that exist at the base and every differing line classified:

```
packs compared: 25
  identical output ................................ 3   (broken_E00, broken_E03, broken_E04 — fail at load)
  differ by W08 blocks + summary line only ........ 22
  any other difference ............................  0
```

Zero behavioural change anywhere on any pack that does not use the new fields. That is not a change
that happened not to break anything; it is a change scoped so that it could not.

## A2 · the prohibited diffs, and every file the branch touched

```
$ git diff dfa3bc3..1d1444f -- backend/packs/                     (empty)
$ git diff dfa3bc3..1d1444f -- backend/app/casepack/models.py     (empty)
$ git diff f5125f4..1d1444f -- backend/packs/ …/models.py         (empty)
```

Empty from **both** candidate baselines — `dfa3bc3` (the real merge-base) and `f5125f4` (the commit
`rework-2.md` §1 nominates). `dfa3bc3` adds only `rework-2.md` and the builder prompt, so the choice
of baseline hides nothing; the builder cut from `dfa3bc3` and said so in its header.

Every file changed, and the ruling on each:

| File | ± | In scope? | Basis |
|---|---|---|---|
| `backend/app/casepack/validate.py` | +132 −25 | **yes** | items 3.1–3.5, the packet's subject |
| `backend/app/casepack/validate_messages.yaml` | +12 −0 | **yes** | `W08` needs a `codes` entry or it cannot be constructed; the `E21` presence reason is item 3.3's |
| `backend/app/casepack/loader.py` | +4 −1 | **yes**, see below | item 3.6, explicitly scoped by §3.6 |
| `backend/tests/check_fixture_matrix.py` | +70 −33 | **yes** | §4 *"must assert both halves of every pair"*; §9.2 makes it the re-run artifact |
| `backend/tests/fixtures/packs/` — 5 new packs, 2 files of `minimal_valid` | +973 | **yes** | §4's table; the `minimal_valid` edit is declared substitution 3, ruled at `1.2-038` |
| `handoffs/1.2-validator/spec.md` | +3 −1 | **partly** | one line authorised, two were required — `1.2-032`; no third line was taken |
| `handoffs/1.2-validator/dod-rework-2.md` | +369 | **yes** | §6 requires it, as a new file; `dod.md` is untouched |

**`loader.py`, ruled.** §3.6 authorised one line and said *"if the fix turns out to be more than a
line, stop and report."* The diff is **one changed statement plus a three-line comment**:

```diff
-    return _read_yaml(path)
+    # Finding 1.1-r2-001: an empty or comment-only file reads as {}, which is not a list, so
+    # creating the file before authoring it destroyed the whole pack with E00 -- the exact
+    # opposite of what an optional section means, and step one of closing CG-5.
+    return _read_yaml(path) or default
```

**Ruling: compliant.** The executable change is exactly the one line `1.1-r2-001` specified as its
closure condition (*"`return _read_yaml(path) or default`. One line."*), byte for byte. The comment
adds no behaviour and is the kind of citation `SPEC_PROTOCOL §2` asks for everywhere else. Nothing
else in the file moved: `_optional` has exactly one caller (`loader.py:89`, `obligation_rules`,
default `[]`), so the `or` cannot swallow a legitimate falsy value anywhere. The builder did not need
to stop and was right not to.

**Every file the branch changed is in scope.** `models.py`, `checks.py`, `seed.py`, the CLI, the
packs and the twenty pre-existing fixtures are untouched. One commit on the branch; `main` was not
written to; `git status` clean in all three worktrees.

## A3 · `check_fixture_matrix.py` on the branch

```
$ cd backend && python3 tests/check_fixture_matrix.py
… 29 fixture rows, every one PASS …
I1  implemented codes : 29   spec-named codes : 29   set equality : PASS
I5  minimal_valid / warn_heuristics / riverside_grocery / packs (directory mode)  identical=yes
I5  directory-mode pack attribution   every record names its pack
all 29 fixtures behave as named; 28 of 29 codes exercised, ['I8'] recorded as unfixturable
EXIT=0
```

Green, exit 0, all 29 — five more rows than the 24 it carried at `dfa3bc3`. `I2` and `I3` re-run
directly: `broken_E12` → exit 1; `warn_heuristics` → exit 0; `broken_W08` (a `WARN`-only pack) →
exit 0. `I4` re-run: `grep -rniE "riverside|grocer" backend/app/casepack/validate*` → zero.

---

# PART B — the six items, each constructed rather than read

Every result below was produced by building the shape by hand under the scratchpad and running the
branch's CLI on it. Where the shape needed to be shown *not* to work before, it was run against the
`dfa3bc3` tree as well.

## B1 · 3.1 — `E12` exempts presence

`Lens.thresholdless` now filters on `rule.metric_kind == "threshold"` (`validate.py:456-463`). Written
positively rather than as `!= "presence"`, so a third kind would not be silently exempted — a small
thing the builder called and got right.

| Shape constructed | Branch | Base `dfa3bc3` |
|---|---|---|
| `threshold`, both thresholds `null` (`broken_E12`, `broken_E20_mute`) | `E12` fires | `E12` fires |
| `presence`, no thresholds (`ok_presence_rule`, scratch Riverside) | **silent** | `E12` fires |
| `presence` **carrying** `critical_above: 0.9` | pack-wide `E00` at the model — **`E12` is never reached** | same |

The third row is the false-negative §3.1 warns about, and it cannot arise: `models.py`'s
`presence_rules_carry_no_thresholds` rejects it at load, verified in this session —
*"Value error, metric_kind 'presence' must not carry warn_above or critical_above"*. The exemption
therefore cannot hide a real defect. **Item 3.1 does what it claims.**

## B2 · 3.2 — `E20` counts a presence rule as coverage

`_can_raise` (`validate.py:404-417`) returns `True` for any `presence` rule and, for `threshold`,
`warn_above is not None or critical_above is not None`. `Lens.watched_with_threshold` was renamed
`Lens.signal_covered` and `check_unwatched_capabilities` consults it. `grep -rn
"watched_with_threshold" backend/` → zero hits; no caller was left on the old name.

Constructed both halves:

- **still fires** — `broken_E20_mute`, a capability watched only by a thresholdless `threshold`
  rule: `E12` + `E20`, exit 1, identical on both trees.
- **now silent** — the scratch Riverside of Part E: `firm_infrastructure` leaves the `E20` list the
  moment `sec_identity_01` is declared `presence`.

**Item 3.2 does what it claims.**

## B3 · 3.3 — `_raisable` consults `metric_kind`, and decision 10 is the sharp edge

`_raisable_severities` (`validate.py:421-434`) returns `{"critical"}` for a presence rule — not
`{"critical", "warning"}`. This is the item the packet was most likely to get wrong, so it was tested
three ways.

**(1) By construction, the positive edge.** `minimal_valid` with `rec_adopt_01` made `presence` and
one authored event waiting on it at **`critical`**:

```
  0 errors · 0 warnings · exit 0        (E21 silent on the probe event)
```

**(2) By construction, the edge decision 10 forbids.** The same pack with the event waiting at
**`warning`**:

```
  ERROR  E21  Event audit_probe               events.yaml:100
         Can never happen: it waits for 'rec_adopt_01' to reach warning, and that rule watches for
         a condition simply being present, which is always raised as critical and never as a warning.
```

**A presence rule does not satisfy a `warning` precondition.** `1.5 §3` decision 10 and `1.5 §6` I5
are both honoured.

**(3) By mutation — the check that matters.** An over-widened `_raisable` would pass every other
check in this packet, so the branch's own code was mutated to return `{"critical", "warning"}` and
the suite re-run:

```
$ (branch tree, _raisable_severities over-widened)
$ python3 tests/check_fixture_matrix.py
FAIL  broken_E21_presence_warning: did not raise ['E21']
FAIL  broken_E21_presence_warning: exit 0, expected 1
EXIT=1
```

**The suite catches the loosening.** `broken_E21_presence_warning` is the only fixture that does, and
it exists because the builder substituted it for a collapsed pair (`D2` below). That substitution is
what makes decision 10 defended rather than merely implemented.

The forked `E21` reason is correct and readable; its `Fix:` line is not (`1.2-030`).
**Item 3.3 does what it claims.**

## B4 · 3.4 — `Lens.owned` unions the platform services

`validate.py:487`: `for item in [*pack.catalog, *pack.platform.services]`. `EntityDetail` is the
shape both sides use, so no adapter was needed.

Constructed on the pack that matters, not only on a fixture — a scratch Riverside carrying exactly
what 1.3 §3a addition 4 tells 1.3 to author:

```
branch   E02 clears   (errors 15 → 14 on the presence-declared copy)
base     E02 fires    (errors 20, unchanged)
```

And the fixture pair, which isolates it: `ok_service_owns_entity` is `minimal_valid` with a
**three-line** move — `{entity: client, level_of_detail: household}` deleted from the catalog item
and added to the platform service. Clean on the branch (`0 errors · 0 warnings · exit 0`);
`E02 · E22 · E23` on the base. Nothing but the platform service holds `client`, so the fixture
proves the union and nothing else. **Item 3.4 does what it claims.**

## B5 · 3.5 — `W08` at `N = 6`, `WARN`

`check_strategy_draws` (`validate.py:1164`), `W08_MIN_DRAWS = 6` (`validate.py:44`), `severity: WARN` in
`validate_messages.yaml:259-265`. Boundary constructed by hand:

| Draws for a strategy | Result |
|---|---|
| 6 (`minimal_valid`, both strategies) | **silent** |
| 5 (one affinity removed) | `W08 ×1`, `0 errors · 1 warning · exit 0` |
| 0 (`broken_W08`, all seven cards on one strategy) | `W08 ×1`, `W05` **silent**, exit 0 |
| 3 / 2 / 1 / 1 (Riverside) | `W08 ×4`, exit still 1 from the pre-existing errors |

`WARN` confirmed by exit code, not by reading the catalogue: a pack whose only finding is `W08`
exits **0**. The `broken_W08` row is the discriminating one — seven cards for four rounds means
`W05` cannot see the defect and only `W08` can, which is `CG-2`'s literal shape.
**Item 3.5 does what it claims.**

## B6 · 3.6 — the one line in `loader.py`

Constructed all four states of `obligation_rules.yaml` on a copy of `minimal_valid`, on both trees:

| File state | Branch | Base `dfa3bc3` |
|---|---|---|
| absent | 0 errors, exit 0 | 0 errors, exit 0 |
| 0 bytes | **0 errors, exit 0** | `E00`, exit 1 |
| comment-only | **0 errors, exit 0** | `E00`, exit 1 |
| literal `[]` | **0 errors, exit 0** | `E00`, exit 1 |
| one malformed record | `E00`, exit 1 | `E00`, exit 1 |

The explicit-`[]` row is a case `1.1-r2-001` did not name and which the base also failed
(`_read_yaml` returns `yaml.safe_load(handle) or {}`, so `[] → {}`), now fixed by the same line. The
last row is the one that matters for regression: **the fix does not swallow a real content error.**
**Item 3.6 does what it claims, in one line.**

---

# PART C — the paired fixtures, and whether the suite can fail

## C1 · the pairs against `rework-2.md` §4's table

| §4 row | Present | Half it plays | Assertion on the branch |
|---|---|---|---|
| `broken_E12` | yes, pre-existing | illegal | `E12` fires · `E20` **forbidden** (new) |
| `ok_presence_rule` | **new** | legal | **no code at all** (`ANY` forbidden) · exit 0 |
| `broken_E20_mute` | yes, pre-existing | illegal | `E12` + `E20` fire |
| `broken_E21` | yes, pre-existing | unreachable | `E21` fires · `E12`, `E20` **forbidden** (new) |
| `ok_presence_arms_event` | **collapsed** into `ok_presence_rule` | legal | `E21` silent — see `D2` |
| `broken_E02` | yes, pre-existing | illegal | `E02` fires |
| `ok_service_owns_entity` | **new** | legal | **no code at all** · exit 0 |
| `broken_W08` | **new** | illegal | `W08` fires · `W05` **forbidden** |
| `ok_obligations_empty` | **new** | legal | **no code at all** · exit 0, no `E00` |
| — | **new**, not required | decision 10's other edge | `broken_E21_presence_warning`: `E21` **fires** |

**One required pair is collapsed and none is missing.** The collapse is `ok_presence_arms_event`,
declared, ruled at `D2`. The suite carries one row §4 did not ask for, and it is the row that
defends decision 10.

The `ANY` sentinel is a real tightening. The old rows were `(wanted, exit)` and passed as long as
nothing wanted was missing — so a fixture named "passes clean" could have raised anything and still
gone green as long as it exited 0. `minimal_valid`, `ok_presence_rule`, `ok_service_owns_entity` and
`ok_obligations_empty` now forbid **every** code.

## C2 · the key test — the branch's suite against the pre-rework build

Built independently: a copy of the `dfa3bc3` tree with `backend/tests/` (matrix **and** fixtures) and
`handoffs/1.2-validator/spec.md` replaced by the branch's, and `backend/app/` and `backend/bin/` left
at `dfa3bc3`. Confirmed before running that the app code carried no `W08_MIN_DRAWS`, no `_can_raise`,
no `_raisable_severities` and no `or default`.

```
$ cd backend && python3 tests/check_fixture_matrix.py

minimal_valid                   0    0  +[-] -[*] got[-]                        PASS
broken_E00 … warn_heuristics    (24 pre-existing rows)                          PASS
ok_presence_rule                0    1  +[-] -[*] got[E12,E20,E21]              FAIL
broken_E21_presence_warning     1    1  +[E21] -[E12,E20] got[E12,E20,E21]      FAIL
ok_service_owns_entity          0    1  +[-] -[*] got[E02,E22,E23]              FAIL
broken_W08                      0    0  +[W08] -[W05] got[-]                    FAIL
ok_obligations_empty            0    1  +[-] -[*] got[E00]                      FAIL

I1  implemented codes : 28   spec-named codes : 29   set equality : FAIL

FAIL  ok_presence_rule: raised ['E12', 'E20', 'E21'], which this fixture forbids
FAIL  ok_presence_rule: exit 1, expected 0
FAIL  broken_E21_presence_warning: raised ['E12', 'E20'], which this fixture forbids
FAIL  ok_service_owns_entity: raised ['E02', 'E22', 'E23'], which this fixture forbids
FAIL  ok_service_owns_entity: exit 1, expected 0
FAIL  broken_W08: did not raise ['W08']
FAIL  ok_obligations_empty: raised ['E00'], which this fixture forbids
FAIL  ok_obligations_empty: exit 1, expected 0
FAIL  I1: spec names codes this build does not implement: ['W08']
EXIT=1
```

**Verified independently, and the builder's claim is accurate.** Nine assertion failures across all
five new fixtures plus `I1`. Item by item:

| Item | Assertion that discriminates | Fails on `dfa3bc3`? |
|---|---|---|
| 3.1 `E12` exempts presence | `ok_presence_rule` leaks `E12` | **yes** |
| 3.2 `E20` counts presence | `ok_presence_rule` leaks `E20` | **yes** |
| 3.3 `_raisable` (reachable half) | `ok_presence_rule` leaks `E21` | **yes** |
| 3.3 `_raisable` (never-warning half) | `broken_E21_presence_warning` leaks `E12`, `E20` | **yes**, and it fails on the mutated *branch* too (B3) |
| 3.4 `Lens.owned` union | `ok_service_owns_entity` leaks `E02` | **yes** |
| 3.5 `W08` | `broken_W08` does not raise it · `I1` set equality | **yes**, twice |
| 3.6 loader | `ok_obligations_empty` leaks `E00` | **yes** |

**Every one of the six items has at least one assertion that fails in the old world and passes in the
new one.** The 24 pre-existing rows pass in both, which is what they are for.

One nuance worth stating precisely, because it is where a reader could over-claim: on `dfa3bc3`,
`broken_E21_presence_warning`'s **positive** assertion (`E21` fires) passes — for the wrong reason,
since the rule reads as thresholdless there. Its discriminating power against the *base* is the
forbidden `E12`/`E20`; its discriminating power against **decision 10** is the mutation test in B3,
which is the one that actually matters and which the fixture wins.

## C3 · `GOVERNANCE §4.9` on the new fixtures, and the `minimal_valid` question

**Real content, minimally different — passes.** `diff -rq` against `minimal_valid`:
`ok_service_owns_entity` 3 changed lines · `broken_E21_presence_warning` 9 · `ok_presence_rule` 11 ·
`broken_W08` 24 · `ok_obligations_empty` one added file. Every one is the same veterinary group
with one thing moved. `grep -rn "TODO\|FIXME\|lorem\|placeholder\|XXX"` across all five → zero. Every
new record carries a `provenance` block. `ok_obligations_empty`'s file is a three-line comment that
explains itself and is exactly the `1.1-r2-001` shape.

`broken_E21_presence_warning` deserves a word: it is `minimal_valid` with **one field added** —
`metric_kind: presence` on `rec_adopt_01` — and nothing else. `minimal_valid`'s own
`records_left_on_paper` already waits on that rule at `warning`, so the fixture is the smallest
possible pack that puts a presence rule under a warning precondition. That is a good fixture.

**The three cards authored into `minimal_valid`: ruled SOUND.** Full reasoning and the constructed
counter-test at `1.2-038` — the four-card deck raised `W08 ×2` at `N = pack.rounds = 4` as well, so
the cards were owed on the fixture's own terms and not only to fit a constant. The cards are real
content by every test in `§4.9`. The residual coupling — seven cards for a four-round pack — belongs
to `1.5 O4` and is recorded, not waved through.

---

# PART D — rulings on the seven declared substitutions

| # | Substitution | Accurate? | Sound? | Author ruling needed? |
|---|---|---|---|---|
| 1 | `ok_presence_arms_event` collapsed into `ok_presence_rule`; `broken_E21_presence_warning` added | **yes** | **yes** — see `D2` | no |
| 2 | `broken_W08` is a starved strategy, not a short deck | **yes** | **yes** | no |
| 3 | `minimal_valid` gained three cards | **yes** | **yes** — `1.2-038` | no, but see `D3` |
| 4 | `W08`'s `N` is flat, not `pack.rounds` | **yes** | **yes** as a decision; its in-code reason is wrong (`1.2-034`) | **yes** — `D3` |
| 5 | `watched_with_threshold` → `signal_covered` | **yes** | **yes** | no |
| 6 | `E02`/`E23` `Fix:` copy left under-describing | **yes**, and incomplete — `1.2-031` | **no** — `D4` | no |
| 7 | pre-existing fixtures now emit `W08` | **yes** | **yes** — `1.2-036` | no |

Plus the two-line spec edit, declared in §7 rather than §8, ruled at `1.2-032`.

## D1 · two spec lines, not the one authorised

**Ruled at `1.2-032`. The claim is true, verified by construction both ways.** `I1`'s authority is
the header code-list line; a `W08` in §5.3 that the header does not cover raises
`SystemExit("spec.md disagrees with itself")` and the re-run artifact exits 1. Reverting §5.3 while
keeping the header passes. **The instruction's "exactly one line to §5.3" had no compliant route,
and named the line that is not load-bearing.** The finding is against `rework-2.md`, not the build;
the builder took the minimum self-consistent edit, changed nothing else, and declared it with the
mechanism quoted correctly. A second, quieter consequence of the same clause is `1.2-033`.

## D2 · the collapsed pair — was coverage preserved?

**Yes, and it was improved.** The argument turns on whether the two fixtures really would have been
byte-identical, so it was checked rather than accepted.

`ok_presence_rule` is `minimal_valid` with exactly two edits: `metric_kind: presence` on
`rec_adopt_01`, and `records_left_on_paper`'s precondition moved `warning` → `critical`. A fixture
built to §4's `ok_presence_arms_event` line — *"event needing `critical` from a **presence** rule"* —
derived minimally from `minimal_valid`, requires those same two edits and no others.
**They are the same pack.** A second directory would have been a duplicate, and `1.2-004` exists on
this project because a vacuous check survived to audit.

Coverage, checked assertion by assertion. `ok_presence_rule`'s row forbids **every** code, which
subsumes all three of the pair's obligations at once: `E12` silent (3.1), `E20` silent (3.2), `E21`
silent (3.3). Verified on the base that all three would otherwise fire —
`got[E12,E20,E21]`. Nothing the separate fixture would have asserted is unasserted.

And the substitution buys something the two-fixture version would not have had.
`broken_E21_presence_warning` is the **only** row in the suite that fails when `_raisable_severities`
is over-widened (B3), which is precisely the failure mode this packet was warned about. Trading a
duplicate for that is a strictly better use of the slot.

**One cost, recorded not filed:** three of the four pairs' silent halves now hang off one fixture, so
a fault that breaks `ok_presence_rule` for an unrelated reason takes all three signals down at once.
The `ANY` sentinel makes that loud rather than silent, so the cost is diagnosability, not coverage.

## D3 · `W08`'s `N` fixed at 6 while `minimal_valid` plays four rounds

**Flagging it rather than improvising was right, and it is the only defensible call.** `N` is
`1.5`'s `O4` — an **open decision on another packet's spec**, with a stated default and a reporting
obligation. `GOVERNANCE §4.4` and `QUALITY_PROTOCOL §6` both say a builder meeting an unsettled case
stops and reports; `SPEC_PROTOCOL §3` says one source of truth per fact. A 1.2 builder that
re-derived `N` would have resolved a 1.5 open decision on its own authority, and it would have done
so invisibly inside a constant. It implemented the ruling as written, made the edge explicit in
`dod-rework-2.md` §8 item 4, and named the owner. That is the correct handling.

**The correct answer, as far as this audit can determine it, is `N = pack.rounds`** — and it is
`1.5`'s author to make, not this branch's. The reasoning:

1. `O4`'s own text is *"**6, one per round.**"* The number and the rationale agree only for a
   six-round pack. `minimal_valid` plays four, and is held to six.
2. `W05` — the deck-depth check `W08` was specified to complement — already reads
   `lens.pack.metadata.rounds` (`validate.py:1148`). Two deck checks in the same file disagreeing
   about where the round count comes from is a second home for the same fact.
3. `GOVERNANCE §4.6` does not object: reading a pack's declared `rounds` is not case-specific logic,
   and `1.2-034` shows the in-code claim that `I4` forbids it is mistaken.
4. **It costs nothing where it was ruled.** Riverside plays six rounds, so `N = pack.rounds` leaves
   `W08 ×4` at 3 / 2 / 1 / 1 — every number in `1.5 §5.2a`, `rework-2.md` §3.5 and this branch's
   output is unchanged. The only pack whose answer moves is a fixture.

Whoever rules should also settle whether `minimal_valid` keeps seven cards for four rounds
(`1.2-038`).

## D4 · `E02`/`E23`'s `Fix:` copy — recorded but not fixed

**Ruled: correct to declare, wrong to leave — and the declaration is half a step short.**
Filed as `1.2-031`.

Three things push against the builder's reasoning that *"message copy is outside this packet's six
items"*:

1. It edited message copy for the six items anyway, in the same file, for the same reason —
   `severity_unreachable_presence` exists because item 3.3 made an existing message misleading.
   `E02`'s message was made misleading by item **3.4** in the identical way. The line between the
   two is not principled.
2. `E01`'s fix already carries the two-place wording (*"an item in `catalog.yaml` or a service in
   `platform.yaml`"*), so there is no design decision, no new vocabulary and no author ruling — it is
   a transcription.
3. `E02 ×1` is the single error `1.3 §3a` calls out by name as the one 1.3 must clear by authoring
   `central_sign_on.owns_entities` in `platform.yaml`. The message the 1.3 author reads at that
   moment says *"no catalog item can hold it"* and *"add a catalog item"*. `GOVERNANCE §4.10` and
   spec §3 decision 3 both bear on a next action that points at the wrong file.

The declaration also stops one step short: it covers `Fix:` and not `message:`, and the `message:`
half is a statement of fact rather than advice.

Not blocking — it changes no code, no count and no exit. But *"not fixed"* should not be read as
*"acceptable"*: it is a two-string change in a file this branch already edited.

---

# PART E — did this packet do its job?

## E1 · the proof, reproduced independently

**Derived before running, and before reading anyone's explanation of it.** Riverside has 7
capabilities and 3 watch rules. `ord_cap_01` watches `order_fulfilment` and carries both thresholds.
`wh_rollout_01` **also** watches `order_fulfilment`. `sec_identity_01` watches
`firm_infrastructure`. The other five capabilities have no rule at all.

So when both thresholdless rules become legal presence rules:

- `E12 ×2` → 0. Both are exempt.
- `E21 ×2` → 0. Both dead cards wait on `wh_rollout_01` at `critical`, which a presence rule reaches.
- `E20` → **the fall is one, not two.** `wh_rollout_01`'s capability was **already covered** by
  `ord_cap_01`, so promoting it adds nothing. Only `firm_infrastructure` is newly covered. The
  remaining five have no rule to promote. **6 → 5.**

Run — scratch Riverside under the scratchpad, `backend/packs/` untouched, `metric_kind: presence`
added to `wh_rollout_01` and `sec_identity_01` and **nothing else**:

```
$ ./bin/validate_casepack <scratch>/rv_presence
  E07 ×8 · E20 ×5 · E02 ×1 · E14 ×1 · W02 · W04 · W05 · W08 ×4
  15 errors · 7 warnings · exit 1

$ … | grep -E "E12|E20|E21"
  ERROR  E20  Customer Insight        ERROR  E20  Financial Reporting
  ERROR  E20  Marketing and Sales     ERROR  E20  Service
  ERROR  E20  Store Operations
```

`E12 ×2` **vanished**. `E21 ×2` **vanished**. `E20` **6 → 5**, and the capability that left the list
is `Firm Infrastructure`, as derived. `E02 ×1` still fires, exactly as `rework-2.md` §3.4 said it
would. **The experiment that failed after 1.1's rework passes.**

Carried one step further, because the interesting question is whether 1.3 can now *finish*. The same
pack plus `central_sign_on.owns_entities`:

```
branch   E07 ×8 · E14 ×1 · E20 ×5        14 errors
base     the untouched 20
```

The 14 that remain are `E07 ×8` (eight missing label keys, `1.2-012`), `E14 ×1` (`CG-6`) and
`E20 ×5` (five capabilities with no watch rule at all, `CG-1`) — **every one of them pure
authoring**, on 1.3's list, closeable with no further code.

## E2 · is 1.3's gate lifted?

# **YES — for `I6`, `I8`, `I9` and `I12`. NO for `I11`.**

`1.3 §3a`'s box *"The 1.1 gate is lifted. It was not enough"* is answered. Every one of the eleven
errors it said were unreachable is now reachable by authoring, proved above by construction rather
than by inference. `I9` additionally gains an executable check it never had — `W08` is exactly its
predicate at exactly its `N`.

Still outstanding for 1.3, specifically:

| # | What | Owner | Note |
|---|---|---|---|
| 1 | **`E02 ×1` still fires on Riverside as shipped**, and is *expected* to | **1.3** | It clears when 1.3 authors `central_sign_on.owns_entities` in `platform.yaml`. Proved above: `E02` clears on this branch with that one addition and does not clear on the base. This packet made it possible; it did not clear it, and `spec.md` §5.5 / §9.1 still list it |
| 2 | `1.3 §3a`'s box is now stale | author | It says `I6` *"is unreachable until 1.2's next rework lands"* and *"`E02` cannot be cleared by this packet"*. Both were true at `b4002be`; after this merge `I6` is reachable and `E02` is 1.3's to clear |
| 3 | **`I11` has no executable check** | 1.2 (unlisted) · 1.5 | `1.2-037`. A fully orphaned `obligation_rules.yaml` validates identically to a correct one. Not among this packet's six items and on no handoff list |
| 4 | `1.2-024` — eight codes still lead with a machine key | 1.2 (unlisted) | `1.1-r2-003` item 8, re-verified here: `models.py:460-463` carries `entities` / `catalog` / `watch_rules` / `questions`, while `grep -o '\.label("[a-z_]*"' validate.py \| sort \| uniq -c` returns only `capabilities ×7 · roles ×1 · stakeholders ×1 · strategies ×2`, and the eight subjects are fed raw keys (`validate.py:614, 711, 779, 840, 897, 1124, 1139, 1212, 1228`). On 1.3's DoD and **not closeable by authoring** |
| 5 | `E02`/`E23` point the 1.3 author at the wrong file | 1.2 | `1.2-031` |
| 6 | `W08` vs `W03` | author, then 1.3 | `1.2-035`. Closing `CG-2` with empty-affinity cards trades one `W08` for one `W03` per card |

**A 1.3 dispatch no longer needs a deferral clause for `I6` or `I8`. It still needs one for `I11`**,
and `§3a` should say so rather than leaving the reader to infer that "the gate is lifted" means all
of it.

## E3 · is 1.5's gate lifted?

# **NO — still partially lifted only, and it should not be dispatched yet.**

`1.5 §10`'s to-1.2 list is now **discharged**, which was not true before this branch:

| to-1.2 item | State |
|---|---|
| 1 `E12` exempts presence | **delivered**, B1 |
| 2 `E20` widens to "can raise" | **delivered**, B2 |
| 3 `W08` at `N = 6` | **delivered**, B5 |
| 4 presence-with-threshold as an ERROR | first clause is dead code by construction (`1.1-r2-007`); the model rejects it at load. Correctly not built and declared. Second clause is `E12`, which exists |
| 5 `Lens.owned` unions services | **delivered**, B4 |
| 6 `_raisable` consults `metric_kind` | **delivered**, B3, including the never-warning half |

`1.5 §6` `I5` — *"a `presence` rule can only ever attain `critical`"* — is now a property the
validator actually enforces, so 1.5 inherits it rather than having to build it.

What remains, none of it this packet's:

| # | Outstanding | Owner |
|---|---|---|
| 1 | **Pre-flight row 8** — *"Riverside validates with no `E12`, no `E20`, no `E21`" → STOP* — still fails, correctly. It depends on 1.3, which can now proceed | 1.3 |
| 2 | `placement` on `EventPrecondition` (`1.1-r2-002`) and a **second policy key** for `policy_contradiction` (`1.1 rework-2 R3`) — `1.5 §10` item 3a. **Three of six precondition types remain unexpressible**, so build step 3 hits a wall | author ruling, then 1.1 |
| 3 | A closed `PRECONDITION_TYPES` set with per-type field checking — on no list (`1.1-r2-005`) | 1.2 |
| 4 | Obligation cross-reference checks — on no list (`1.2-037`) | 1.2 |
| 5 | `1.5 §5.4`'s `obligation_rules:` wrapper does not load (`1.1-r2-008`) | author |
| 6 | `O4` — does `N` track `pack.rounds`? (`D3`) | author |
| 7 | `W03` vs `§5.2a`'s empty affinity (`1.2-035`) | author |

Items 2 and 3 are the ones that stop a builder mid-packet, and both predate this branch.

---

## Result

```
Run date:   2026-08-18      Auditor:  Claude (fresh context)

Substance:  PASS WITH FINDINGS
            Six items asked for, six delivered, every one confirmed by constructing the
            shape rather than reading the code. The governing rule holds under a far
            stronger test than the DoD applies: 25 packs, both validators, every differing
            line classified - 22 differ by W08 blocks and the summary line, 3 are
            identical, 0 differ any other way. Riverside's ERROR profile is byte-identical,
            not merely count-identical; warnings 3 -> 7 with the four W08 counts exactly as
            1.5 section 5.2a predicted. Prohibited diffs empty from both candidate
            baselines. loader.py is one changed statement, matching 1.1-r2-001's stated
            closure condition byte for byte, and it does not swallow a real content error.
            Decision 10 is right AND defended: an event waiting for `warning` from a
            presence rule still raises E21, and the fixture that asserts it fails when
            _raisable_severities is mutated to over-widen. The proof this packet exists for
            reproduces - E12 x2 and E21 x2 vanish, E20 falls 6 to 5, and the fall is one
            because wh_rollout_01's capability was already covered. What is short: E21's
            Fix: line now contradicts its own reason, and E02/E23 still point the 1.3
            author at catalog.yaml for a remedy that lives in platform.yaml.

Instruction: PASS WITH FINDINGS
            rework-2.md is a strong instruction. It states the governing rule as a
            prediction rather than a permission, it makes the paired fixtures the centre of
            the packet rather than an afterthought, and its scoping of loader.py is argued
            in the document so the auditor can rule on it - which is the right way to scope
            another module's file. Two defects: the spec-edit authorisation had no
            compliant route (1.2-032), naming the line that is not load-bearing and
            forbidding the one that is; and "nothing else in the spec" also forbade the
            version bump and changelog entry GOVERNANCE section 8 requires (1.2-033).

Reporting:  PASS
            dod-rework-2.md is accurate everywhere I could check it, including its two
            hardest claims - the C2 falsification run and the "the fall is one and not two"
            derivation, both of which I reproduced from the primary artifact before reading
            it. Seven substitutions declared, seven sound. The two-line spec edit is
            declared in section 7 with the mechanism quoted correctly rather than slipped
            in. Two gaps, both in reporting rather than in the build: E21's Fix: line
            (1.2-030) is undeclared while its sibling is declared, and substitution 6
            declares the Fix: half of E02/E23 without the message: half.

Findings:   0 Blocking · 2 Functional · 2 Data · 5 Report   (1.2-030 ... 1.2-038)
```

**Substance and process do not diverge on this branch.** Scope held to six files plus the fixture suite, every one in scope; the prohibited diffs are empty from both baselines; `main` was not
written to and no pack was touched. Where the packet exceeded its letter it did so because the letter
was unsatisfiable, and it said so before it was asked.

Where process and substance *do* diverge is one level up: the **instruction** authorised an edit that
could not be made as written (`1.2-032`) and forbade the document hygiene the project's own
governance requires (`1.2-033`). Both are cheap to close and neither is the builder's.

### MERGEABLE: **yes**

```
$ git merge-tree --write-tree dfa3bc3 1d1444f
d4595e86718e666dc454f63a2decc019cfc973c6      rc=0 · conflicts: 0
$ git ls-remote origin refs/heads/build/1.2-rework-2
1d1444fd9b6c165785bb31161486fd35c26a9c48      == local HEAD
```

The branch is one commit ahead of `main`, which is its own merge-base, so the merge is a
fast-forward. Nothing here is blocking; every applicable `QUALITY_PROTOCOL §5` box is checked or
declared N-A with a reason; and `GOVERNANCE §5`'s hard gate — *"no casepack reaches a section until
`validate_casepack` passes clean"* — is proved unmoved on 25 packs, with the one intended addition
being a `WARN` that does not change an exit code anywhere.

**The smallest set of changes that would make it mergeable: none.** It is mergeable as it stands.

**The smallest set that would make it complete:** `1.2-030` and `1.2-031` — three strings in
`validate_messages.yaml`, in a file this branch already edits, in `E01`'s existing phrasing. Both
land before or after the merge without touching anything else. Everything remaining is an author's:
the two instruction defects, `O4`, and the `W03`/`W08` question.

**Holding it back costs more than merging it.** 1.3 is the critical path and has been gated on this
packet since 2026-08-17; three downstream packets read `spec.md`'s code list; and the two open
message defects change no behaviour a builder or a validator depends on.

### What must be true before 1.3, not before merge

| # | Against | Owner | Closes when |
|---|---|---|---|
| **`1.2-031`** | BUILD | 1.2 | `E02`/`E23` name `platform.yaml`'s services. **This one is worth doing first** — it is the first sentence 1.3 reads about the error `§3a` singles out |
| **`1.2-030`** | BUILD | 1.2 | `E21` gains a `fix` variant for the presence arm |
| **`1.2-037`** | 1.3 · 1.5 | author, then 1.2 | Obligation cross-reference checks reach a list and get built; **1.3's dispatch must state that `I11` is deferred** |
| `1.2-024` | 1.2 | 1.2 | The eight machine-key subjects route through `lens.label` |
| **`D3` / `O4`** | 1.5 | **author ruling** | `N` is settled as flat or as `pack.rounds`; `1.2-034` and `1.2-038` close with it |
| **`1.2-035`** | 1.5 · 1.3 | **author ruling** | Empty `strategy_affinity` is ruled a legitimate shape or a smell; `W03` or `§5.2a` gives way |
| `1.2-032`, `1.2-033` | REWORK INSTRUCTION · SPEC | author | The spec-edit clause names the header; `spec.md` bumps to v1.3 with a changelog row |
| `1.3 §3a`'s stale box | 1.3 | author | *"`I6` unreachable"* and *"`E02` cannot be cleared"* are both false after this merge |

**The one that changes a dispatch decision is `1.2-037`**, and only that one. `I6`, `I8`, `I9` and
`I12` no longer need a deferral clause; `I11` still does.

### On the packet

Two things are worth saying plainly.

The first is about the fixtures. `rework-2.md` §4's insistence that *"a suite that only proves errors
still fire has not tested this packet at all"* was the right thing to put at the centre, and the
builder took it further than the letter required: the `ANY` sentinel turned four "passes clean" rows
from assertions about an exit code into assertions about the whole finding list, and the substituted
`broken_E21_presence_warning` is the only artifact in the repository that would catch an
over-widened `_raisable`. I know it would, because I over-widened it. A packet whose central risk was
a subtle loosening of decision 10 shipped the one test that fails when it loosens.

The second is about what "the gate lifts" has meant across three packets. 1.1's rework landed the
fields and the gate did not move, because a schema addition the validator has not been taught to read
changes what a pack may *say*, not what the gate *reports*. This packet is the other half, and it is
the first one where the sentence is true in the direction it was always written: declare
`metric_kind: presence` on two rules and add one line to `platform.yaml`, and Riverside goes from
twenty errors to fourteen — every one of the fourteen a thing a person can author. Nothing further
between the schema and `CG-1`.
