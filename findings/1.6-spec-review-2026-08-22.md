# Spec Review — 1.6 Round Runner (SPEC_PROTOCOL §11)
Date: 2026-08-22 · Reviewer: Claude (independent, did not author) · Spec: `handoffs/1.6-round-runner/spec.md` v1.1
Target: branch `author/1.6-reconcile` @ `60746bb` · Base main `3e8f74a`

## Verdict: **PASS WITH FINDINGS** (ready to dispatch a builder)

Bounded consistency pass gating dispatch. The v1.1 reconciliation is internally coherent
and matches the merged 1.4/1.5 engine, the frozen contracts, and the recorded O4 ruling. One
Report-level clarity finding (non-blocking). No STOP condition found.

## What I checked (§11 checklist)

1. **Inputs 1.6 produces map to real defaulted engine fields, shapes exact — PASS.**
   `backend/app/engine/state.py`: `ArchNode.placement: str | None = None` (`:49`);
   `ActionRecord(action_type, locked_round, capability=None, target_key=None, cost=0)` frozen
   dataclass (`:118-134`); `TeamState.action_history: tuple[ActionRecord, ...] = ()` (`:213`);
   `available_funds_by_round: tuple[int, ...] = ()` (`:218`);
   `debt_ratio_by_capability: dict[str, float] | None = None` (`:222`). Every shape in spec
   §3 dec 9 / §5.1a / §7 rows 6–7 matches the code exactly. `checks.ACTION_TYPES` is the closed
   ten-key set (`checks.py:13-24`) — matches spec.
2. **Named 1.5/1.4 entry points exist with stated signatures — PASS.**
   `ledger.advance_ledger(prior, state, pack, fired_signals=frozenset())` (`ledger.py:249`);
   `ledger.project_signal_state(ledger)` (`:101`); `events.resolve_events(state, pack, ledger=(),
   already_fired=frozenset()) -> (fired, suppressed)` (`events.py:102`);
   `events.failed_node(event, state, pack) -> str|None` (`:159`);
   `events.failover_exists(state, failed, capability, pack)` (`:179`);
   `events.outage_duration(state, failed, capability, pack) -> dict` (`:209`);
   `score.score_team(pack, state)` (`score.py:115`). The §5.2 call sequence and arg order
   (incl. `frozenset(fired)` into `fired_signals`) are correct.
3. **`signal` persistence table = frozen `LedgerSignal` shape; PK/append-only coherent — PASS.**
   `ledger.py:29-46` lists the 14 fields incl. `episode_id`; CONTRACTS.md canonical entry
   (`:411-414`) matches. Spec §5.1 columns add `instance_id/team_id/round`; PK
   `(instance_id, team_id, key, episode_id)` with append-only re-raise → `episode_id+1` matches
   CONTRACTS `:416-418` and the engine's carry-forward-byte-identical logic (`ledger.py:263-321`).
4. **O4 folding internally consistent — PASS.** §4 O4 is RULED (2026-08-22, user): advance+project
   the ledger (step 10) before any responsiveness-bearing score; step 9 = raw tech/org/mgmt only;
   step 12 = single authoritative re-score reading `signal_responsiveness` from the freshly-advanced
   ledger. §5.2 step 9 (`NO responsiveness here`), step 12 (`single re-entry`, reads responsiveness
   from advanced ledger), and build-step-0 all agree. No residual "STOP/ruling required" language
   contradicts the recorded ruling; the changelog §11 O4 line is historical (describes v1.1's route
   to the authority), not a live contradiction. Watch-rules-before-events (step 10 < 11) is consistent
   with `1.5 contract-spec §3` (metrics read the round-close snapshot before event outcomes).
5. **New invariants ship real falsification checks; I5 grep addressed — PASS.**
   I9 plant (re-raise a cleared `(key, ep1)` → `(key, ep2)` exists, `ep1` byte-unchanged) mirrors the
   real engine `test_cc5_recurrence_opens_new_episode` (`tests/test_signal_engine.py:102`) — fails if
   the runner overwrites a prior episode. I10 plant (mutate an `available_funds_by_round`/
   `action_history` value that changes the R3 projection → pin drifts) is a real falsification against
   `test_engine_scoring.py:34-40`. The spec explicitly flags its own I5 grep (`grep -c "rescore" == 1`)
   as self-referential (§6 note, lines 355-359) and mandates a planted-second-re-score property test —
   adequately addressed.
6. **Cross-doc consistency — PASS.** Pin consumed, not redefined: spec §3 dec 12 cites
   `0.750008/0.507003/0.656778/0.249744` verbatim; `test_engine_scoring.py` green at base
   (10 passed). 1.5 `contract-spec §9` (`:543-547`) shows action_history/available_funds/
   debt_ratio/placement owned by 1.6 → 1.5 consumes — matches spec; no ownership drift. OPEN-REGISTER
   §M CC-D6/7/8/10 (`:345-386`) confirm 1.6 ownership unchanged and record this exact reconciliation;
   §O S1/S5 (fix-price = 1.7 calibration; `debt_above` unreachable v1, raises `MissingRoundInputError`)
   agree with spec §5.3 and §3 dec 9. `RoundResult` event/outage dict shape (§5.3) matches
   `events.py:230-238`.
7. **Scope discipline — PASS.** Bounded reconciliation; no silent 1.6 scope expansion. The one genuine
   conflict (ordering vs. the projection seam) was surfaced as O4 STOP to the authority per
   GOVERNANCE §7, then ruled by the user — not silently resolved.

## Findings

### Report
- 1.6-SR-001: The O4 folding makes step 12 authoritative for the full per-capability score
  (incl. responsiveness) while step 9 "supplies only raw tech/org/mgmt terms." `score.score_team`
  returns a complete `TeamScore` whose mgmt term always includes `signal_responsiveness`; the spec
  does not spell out the mechanism by which step 9 yields "raw terms only" (i.e., whether step 9
  calls `score_team` and its responsiveness output is discarded/overwritten by step 12, or step 9
  derives the pre-event terms another way). Harmless — step 12 overwrites — and within builder
  latitude, but a one-line clarification in §5.2 would remove ambiguity. Non-blocking.

## Confirmations run this session
- `git rev-parse HEAD` → `60746bb`; base main `3e8f74a`.
- `pytest tests/test_engine_scoring.py -q` → 10 passed (pin green at base).
- grep of entry points, `ACTION_TYPES`, `LedgerSignal` fields, `state.py` shapes — all as cited above.
