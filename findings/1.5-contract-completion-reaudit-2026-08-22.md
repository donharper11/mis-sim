# Re-audit — 1.5 contract-completion specification (v1.1)

**Date:** 2026-08-22
**Auditor:** independent re-auditor (did not author the spec; did not write the v1.0 audit)
**Candidate:** `author/1.5-contract-completion` @ `de901fe69eef66c2ec722160a506f6f08b94240a`
**Base:** `main` @ `de1af0304b2a32968307cbdad031f46e9373db13`
**Lineage:** `a38b940` (v1.0 candidate) → `a8ace7a` (v1.0 audit FAIL) → `de901fe` (v1.1 revision) — the branch shows candidate → FAIL → revision, as required.
**Prior audit:** `findings/1.5-contract-completion-2026-08-22.md` (CC-A-001..007; six Blocking + one Report).
**Protocol:** SPEC_PROTOCOL v1.3 §11 + handoff-gate re-audit; QUALITY_PROTOCOL §4 format.

## Verdict: **PASS WITH FINDINGS**

All six Blocking findings and the one Report finding of the v1.0 audit are **genuinely CLOSED**, verified against the working tree with independent evidence (not the author's word). The revision introduced **two Report-level miscitation defects** (RA-001, RA-002) — the cited *facts* are correct and independently verifiable, but the `file:line` pointers in §5.5.2/§5.5.4 resolve to lines beyond the files' length. Neither is a contract defect, neither threatens the 1.4 pin, and neither makes any contract unsatisfiable. The contracts are correct, total, jointly satisfiable, and safe to dispatch a builder against once the two miscitations are corrected (Report severity — does not block dispatch).

### Environment checks (all clean)
- HEAD == `de901fe`; branch `author/1.5-contract-completion`; **remote tip == `de901fe`** (`git ls-remote`).
- **`main` untouched** at `de1af03`; merge-base(main, HEAD) == `de1af03`; candidate is docs-only over the base (CONTRACTS.md, OPEN-REGISTER.md, contract-spec.md, spec.md, three prompt files, the v1.0 findings). **No backend/pack file changed.**
- **`make check` → exit 0, "all guards green"** (pytest + every `check_*.py` + fixture matrix).
- **`git diff --check` clean.**
- **1.4 pin green:** `tests/test_engine_scoring.py` 10 passed — tech `0.750008`, org `0.507003`, mgmt `0.656778`, realised `0.249744`, throttle `org` (all 1e-6). The pin is computed from the hand-built 4-field `SignalState` seed rows and is untouched by construction (the projection rule is a future contract, not live code).

---

## Per-finding closure

| Finding | v1.0 severity | Re-audit | Evidence |
|---|---|---|---|
| CC-A-001 | Blocking | **CLOSED** | One total rule, hand-recomputed |
| CC-A-002 | Blocking | **CLOSED** (see RA-001) | Full action/funds/target/effectful model |
| CC-A-003 | Blocking | **CLOSED** | `<2`-option guard before division |
| CC-A-004 | Blocking | **CLOSED** | Ordered attribution + total binding |
| CC-A-005 | Blocking | **CLOSED** | Schema prerequisite sequenced; row 7 hard STOP |
| CC-A-006 | Blocking | **CLOSED** | Falsifiable register; CC8 grep fixed (tested) |
| CC-A-007 | Report | **CLOSED** | Prompt in-tree, byte-identical to `2f68317` |

### CC-A-001 — responsiveness projection — **CLOSED**
§5.4 freezes **one total rule**: `acted_before_fire = (cleared_round is not None) and (fire_round is None or cleared_round <= fire_round)`. I recomputed the three-row seed projection **by hand** against `backend/seeds/riverside_r3.py:193-199`:

| signal | cleared_round | fire_round | corrected rule → | seed row |
|---|---|---|---|---|
| `ord_cap_01` | 3 | None | `True` (clear prevents fire — best case) | `acted_before_fire=True` ✓ |
| `wh_rollout_01` | None | 3 | `False` | `acted_before_fire=False` ✓ |
| `sec_identity_01` | None | None | `False` | `acted_before_fire=False` ✓ |

All three `actionable=True`; numerator `acted=1`, denominator `3` → `1/3 = 0.333333`, which is exactly what `_signal_responsiveness` (`backend/app/engine/management.py:137-143`) consumes to produce the frozen `mgmt 0.656778 / realised 0.249744` (verified live: pin test green). The rule is **total** over `{int,None}×{int,None}` (five cases enumerated; each returns a boolean, none undefined). The §5.2 `status` decision procedure is likewise total (if/elif/else) and consistent with the projection (whenever `acted_before_fire=True`, `status="cleared"`; a lapse yields `status="cleared"` with `cleared_round=None` → no credit — honest). The v1.0 rule applied to the same history yields `ord_cap_01 → False`, `0/3 = 0.0`, pin drift — the falsification differs *exactly* on the clear-before-fire row, as claimed. The `CONTRACTS.md` `LedgerSignal` projection line (`CONTRACTS.md:421-425`) was corrected in the same change (diff confirms `fire_round is not None`-required → `(fire_round is None or cleared_round <= fire_round)`).

### CC-A-002 — action history / clearing scope / affordability — **CLOSED** (with RA-001)
§5.5 now freezes all four gaps the v1.0 audit named:
- **Immutable `ActionRecord`** (§5.5.1): `action_type, locked_round, capability, target_key, cost`; carried as `TeamState.action_history: tuple[...] = ()` (explicit default preserves every constructor and the pin — mirrors existing empty-tuple defaults).
- **Committed-spend question decided** (§5.5.2): funds = *remaining* capital, committed spend **already deducted** (`capital_remaining = capital_available − capital_committed`; `220000 − 174000 = 46000` — values present at `pack.yaml:39-40,104`, verified).
- **Exact target matching** (§5.5.3): three conjuncts (action type ∈ `R.cleared_by`; capability match or firm-wide `serves`; locked while open). `ord_cap_01.cleared_by = [scale_node, add_node, move_to_cloud]` confirmed in `watch_rules.yaml`. Lapse path defined (no matching action → `cleared_round=None`, no credit).
- **Effectful-option filter** (§5.5.4): `add_training` uses cheapest option **where `coverage > 0`**, explicitly excluding the authored `none: {cost: 0, coverage: 0.0}` option (verified at `catalog.yaml:58` store item and `:80` order_mgmt_v42); cheapest *effectful* training for order_fulfilment is `basic 12000`, not `0`. `saas capex 0` modes (verified `catalog.yaml:71`) remain effectful candidates. Falsification (plant a `training.none`-only set → `cheapest_fix=None`, `was_actionable=False`) is required as CC7.

Executable and unambiguous. **RA-001** applies (miscited pack.yaml lines).

### CC-A-003 — `policy_contradiction` div-by-zero — **CLOSED**
§6.1 freezes: **if either referenced policy has `len(options) < 2`, the precondition is `FALSE`, guard checked BEFORE any division** — so `n = len-1 = 0` never divides. Covers both valid shapes the v1.0 formula broke on: the one-option policy (`n=0 → 0/0`) and the **zero-option / legacy** policy (`options == []`), whose loading I confirmed live — `backend/tests/check_policy_options.py` PASSes "legacy PolicyOption … options==[] default==None" and the `ok_obligations_empty` legacy-shape pack, both green in `make check`. Boundary decisions stated (`i/n == 0.5` → restrictive half). Required boundary tests (one-option → FALSE no ZeroDivisionError; zero-option → FALSE) are in the §6.1 falsify list and §11 CC10. Total for every pack.

### CC-A-004 — event ownership / blast-radius binding — **CLOSED**
§7.1 replaces the set with an **ordered tuple** derivation (walk `preconditions` in authored order, de-dupe preserving first occurrence; set iteration explicitly prohibited for determinism). **Primary capability = first element**; single recorded suppression `capability` = primary (§7.3); **empty-sequence path** defined (`()` → primary `None`, O2-exempt). §8.1 freezes a **total** `failed_node` binding returning a node **or `None`** for every event (SPOF pc → its node; else primary-cap bottleneck via deterministic `serving_path` order; else `None`/empty radius, no duration). All cited graph helpers exist (`graph.py`: `serving_path:85`, `bottleneck_capacity:113`, `articulation_points:127`, `blast_radius:180`). I found **no remaining non-total case**. Grounded for Riverside: all 13 events use only `signal_open` with **zero `node_is_spof`** (verified `events.yaml`), so each binds via rule 2 or returns `None` — deterministic.

### CC-A-005 — schema gate vs dispatchable scope — **CLOSED**
The contradiction is genuinely resolved by the author-authority route the v1.0 audit named as preferred: §0.5 S7 / §9.1 / §12 row 7 **reclassify** `CatalogItem.base_rto_hours` (default 8.0) and `Capability.agreed_availability` (default 0.99) from a "deferred path" to a **hard 1.1/1.2 prerequisite sequenced before dispatch**, exactly like the readiness closeout. Pre-flight row 7 is rewritten from a deferral into a **hard STOP** (absent fields ⇒ prerequisite not merged ⇒ do not build). The "all five metrics must exist before executable" acceptance rule (`spec.md:107-110`) is **kept unchanged**. A builder can now satisfy both: the two defaulted fields land first, after which all five metrics are buildable for every pack, and no deferred-path escape remains. `OPEN-REGISTER §M` CC-D3/CC-D4 retagged to HARD PREREQUISITE consistently (verified).

### CC-A-006 — §11 falsification register — **CLOSED**
The register is rebuilt into 14 rows (CC1–CC14), each deriving a **falsifiable planted-defect** from a load-bearing contract, including the invariants the v1.0 register omitted: recurrence/immutability (CC5), lifecycle-status ordering (CC6), action-target matching + effectful price (CC7), the eleven precondition boundaries (CC10), multi-cap suppression ordering (CC11), total failed-node binding (CC12), `arms` gating (CC13), and duration evidence (CC14). Each names a concrete planted input and a concrete observable assertion failure — not ritual. The checkable rows were **exercised now**:
- **CC8 grep fixed and tested:** `git ls-files backend/app/engine | xargs grep -nE "riverside|grocer|random\.|datetime\.now|session|open\("` → **zero hits** on the real engine. On a synthetic file containing `open("f")`, `session.get(1)`, and `if pack=="riverside"`, the **new grep detects all three**, while the **v1.0 grep misses `open(` and `session`** — the canary now catches what it claimed to. Verified by direct execution.
- **CC4 arith:** the three-row projection reproduced by hand (above) — corrected rule `1/3`, v1.0 rule `0/3`.
- **CC9:** `make check` / pin test green.

### CC-A-007 — dispatch scope in-tree — **CLOSED**
`handoffs/_prompts/1.5-contract-completion.txt` is present at HEAD and **byte-identical** to `git show 2f68317:handoffs/_prompts/1.5-contract-completion.txt` (`diff` → IDENTICAL). The immutable source `2f68317` is cited in §0. Independently auditable with no cross-branch lookup.

---

## New findings introduced by the v1.1 revision

### RA-001 — Report — pack.yaml capital citations resolve past end of file (§5.5.2)
§5.5.2 cites the committed-spend derivation as `pack.yaml:595-612`, `pack.yaml:612-613,677`, and `pack.metadata.budget.capex_per_round` at `pack.yaml:587`. **`backend/packs/riverside_grocery/pack.yaml` is 114 lines.** The *facts* are correct and present — `capex_per_round` at line 14, `capital_remaining: 46000` at line 39, `capital_available: 220000` at line 40, `capital_committed: 174000` at line 104; `220000 − 174000 = 46000` holds — but the cited lines do not exist. This violates the spec's own bar ("every field, value, and threshold … carries a `file:line`") and could send a builder to a non-existent location. Not blocking: the values verify by grep and the affordability contract is sound. **Fix:** correct the citations to lines 14/39-40/104.

### RA-002 — Report — platform.yaml citations resolve past end of file (§5.5.4)
§5.5.4 cites `platform.yaml:523-570` (support/integration tiers) and §5.5.4's worked example cites `platform.yaml:439` (`central_sign_on saas capex 0`). **`backend/packs/riverside_grocery/platform.yaml` is 241 lines.** Facts correct: `central_sign_on` at line 103 with `saas: {capex: 0, …}` at line 108; `support_tiers` at line 192; `integration_tiers` at line 236. Same class as RA-001 — wrong line pointers, right values, right file. Not blocking. **Fix:** correct to lines 103-108/192/236.

*(Note: all `catalog.yaml` (414 lines), `models.py`, `graph.py`, `state.py`, `watch_rules.yaml`, `events.yaml`, and `demo.py` citations spot-checked were accurate. The miscitation is confined to the two pack top-level YAMLs cited in §5.5.)*

---

## Observations (not findings)
- **Fresh-author attestation not git-verifiable.** §GOVERNANCE §6.2 requires the v1.1 revision be a different author than v1.0. Both `a38b940` and `de901fe` carry the same git identity (`donharper11`), so distinct authorship is a self-attestation I cannot confirm from metadata. This does not affect the contract verdict.

## Verified-honest disclosures (unchanged, re-checked)
- **S1:** `ORDER_THROUGHPUT = 7225.0` (`riverside_r3.py`); `12000 / 7225 = 1.6608996…`, not `1.11`; formula frozen, numbers deferred to 1.7 calibration. Honest.
- **S2:** `saturday_queue_collapse` absent from the deck; demonstration rebound to `warehouse_rollout_gap` — which exists, fires on `wh_rollout_01` critical (`events.yaml`), with a `fund` option `cost: 6000`. Honest.
- **S6:** live pins `mgmt 0.656778 / realised 0.249744` reproduce (pin test green). Honest.

## What I exercised
`git rev-parse`/`ls-remote`/`merge-base` (branch topology); `git diff --stat`/`--check` (docs-only, clean); `make check` (exit 0); `pytest tests/test_engine_scoring.py` (10 passed, pins to 1e-6); `diff` of the in-tree prompt against `2f68317`; the CC8 grep against the real engine (zero) and against a synthetic planted-defect file (detects `open(`/`session`, v1.0 grep misses them); hand-recomputation of the three-row responsiveness projection under both rules; and direct source verification of the seed rows, scorer numerator, `SignalState` shape, `cleared_by`, catalog training/`saas` options, legacy zero-option loading, pack capital arithmetic, platform tiers, event precondition types, and the `CONTRACTS.md` / `OPEN-REGISTER §M` ripple.

**No implementation, contract correction, merge, or push was performed.** The two Report miscitations should be corrected before dispatch, but do not block the handoff gate.
