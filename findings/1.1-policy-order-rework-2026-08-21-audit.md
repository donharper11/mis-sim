# Findings — 1.1 Policy-Order Rework

Date: 2026-08-21 · Auditor: independent Codex session  
Branch: `build/1.1-policy-order-rework` · tip `dd29a76` · base `174e980`  
Current main during audit: `b5f53cd`

## Verdict

**PASS WITH FINDINGS · NOT MERGEABLE until `1.1-PR-001` and `1.1-PR-002` are corrected.**

The core rework is correct: the model, contract, authoring guide and Riverside provenance
now agree that `PolicyOption.options` is ordinal and index 0 is least constrained. The
additional `PROVENANCE.md` edit is necessary, accurately declared and in scope. The branch
merges without conflict into current `main` (`git merge-tree --write-tree main dd29a76` →
tree `a42e98c6…`). No engine file is changed by the true rework diff
(`git diff --name-status 174e980..dd29a76`).

## Functional

### `1.1-PR-001` — the new source and contract falsely say the 1.4 scorer currently reads policy distance

`backend/app/casepack/models.py:443-445` says *“Alignment scoring reads that distance”*.
The new `CONTRACTS.md` consumer row likewise says *“the alignment scorer (1.4, reads ordinal
distance)”*.

Current merged 1.4 proves the opposite:

```text
backend/app/engine/management.py:165-184
policy_switch_alignment(...) raises NotImplementedError; the docstring says the dimension
is deferred and PolicyOption.options is not read.

findings/1.4-2026-08-21-audit.md:50-64
policy-distance pause compliant; no exact-match or distance computation exists.
```

The ordinal contract is settled; its implementation status is not “reads”. Correct both
claims to prospective language, e.g. “alignment scoring will consume this distance when
the deferred policy-switch dimension is implemented.” Do not add scoring code in 1.1.

### `1.1-PR-002` — all nine new checks pass unchanged on the pre-rework base

The re-builder prompt required focused tests that fail against `174e980` and pass at the
branch tip. I applied only `backend/tests/check_policy_options.py` from the branch to a
detached `174e980` worktree and ran it unchanged:

```text
PASS ... (all nine rows)
all 9 policy-option contract checks pass
exit 0
```

This is expected from the test body: it proves the already-correct pack order and loader
behavior, but never inspects the contradictory model documentation or absent contract that
the packet changes. It therefore cannot regress either repaired finding.

Add at least one focused static contract check that fails on `174e980` and passes at the
branch tip by proving the authoritative model/contract surfaces declare the ordinal rule
and do not declare order meaningless. Preserve the useful nine behavioral checks.
Demonstrate the same test file red on `174e980` and green at the corrected tip.

## Independently rerun checks

- `check_policy_options.py`: 9/9 PASS at `dd29a76`.
- `check_fixture_matrix.py`: 29/29 fixtures PASS; code sets equal; text/JSON parity PASS.
- Riverside validator: `0 errors · 0 warnings · exit 0`.
- `git diff --check 174e980..dd29a76`: clean.
- Rework branch worktree: clean.
- True rework diff: six files only; zero 1.4 engine files.
- Merge simulation into `b5f53cd`: clean.

## Closeout required

The same re-builder may make these two mechanical corrections. Append evidence to
`dod-policy-order-rework.md`, commit, leave the branch clean, and return the new tip for a
short independent re-audit. Do not merge `dd29a76` as-is.
