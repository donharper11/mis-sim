# Findings — 0.3 Token Map + Mockup Pilot · Phase 1 gate

**Date:** 2026-07-26 · **Auditor:** Claude (fresh session, independent context)
**Branch:** `build/0.3-mockup-pilot` @ `76d5e3ac4b66f7ff3f96453f9b917eaa5c2e679f`
**Spec:** `handoffs/0.3-mockup-pilot/spec.md` · **Checklist:** `playthrough.md` Part A

> **NUMBERING NOTE (added 2026-07-26).** This report was written before Phase 0 was
> renumbered. Module `0.4a` is now `0.3`; `0.4b` is now `0.4`; `0.6` is now `0.5`.
> Finding `0.4a-012` — the plan-index self-contradiction — is closed by that renumbering.

> **Independence declared** (`SPEC_PROTOCOL.md §2.2.5`): I did not author this spec and did
> not build this branch. `GOVERNANCE.md §6.1` makes a fresh auditor mandatory from 0.3
> onward; this is that pass. I inherited no builder context and treated the DoD as claims
> to verify. Every check below was re-run, not accepted.

**Scope of this pass.** The builder stopped at the Phase 1 gate as instructed. No mockups
exist, so Parts B–E of the checklist are not yet exercisable. This audits Part A (the token
map), Part F (scope), and the consequences of the rewrite on code already in the repo.

**Method notes.** `grep` in the audit shell is ugrep 7.5.0 with `--ignore-files`; every
load-bearing grep was re-run through `command grep` (GNU) as well, and both are reported.
Browser work used Playwright 1.58.2 (snap chromium cannot write outside `$HOME`).

---

## Part A — PASS, all eight checks

Re-run independently by script (`:root` parsed, not eyeballed):

| # | Check | Result |
|---|---|---|
| A1 | Two separated blocks | PASS — primitives block, then `/* Semantic roles … */`, comments state the tier rule |
| A2 | Every role in spec §5.1 declared exactly once | PASS — expected 75, actual 75, **missing NONE, extra NONE, duplicates NONE** |
| A3 | Every `--role: var(--x)` resolves to a primitive, one hop (I2) | PASS — role→non-primitive violations: **NONE**; no primitive references `var()`; `--radius: 0` is direct per spec §5.1 |
| A4 | Deprecation table accounts for all 89 original tokens | PASS — 89 rows parsed; old tokens with no row: **NONE**; rows not in the original file: **NONE**; duplicate rows: **NONE** |
| A5 | `--accent-*` and `--status-*` disjoint | PASS *(see reading below)* |
| A6 | `--chart-*` disjoint from accent and status | PASS *(same reading)* |
| A7 | CONTRACTS.md entry present, states the one-step rule | PASS with a format note — see 0.3-005 |
| A8 | I7 — no tokens declared outside `theme.css` | PASS — zero hits under the spec's command **and** under GNU grep |

**A5/A6 reading, recorded because the check is ambiguous.** "Disjoint" holds at the *role*
level: no accent role is reused as a status role or a chart role. It does **not** hold at
the value level — `--accent-1`, `--chart-1` and `--status-info-marker` all resolve to
`--blue-500`. That is the design, not a defect: spec §5.1 says primitives "may repeat
values freely" and that the point of two tiers is that roles "stay distinct and separately
changeable, and only the primitive is shared." I read A5/A6 as name-disjointness and they
pass. **Amend the checklist wording before 0.4b** so the next auditor does not read it the
other way and file a false finding.

**Value preservation — checked although the spec never asked for it.** For all 89 rows I
resolved the old token's literal value and the replacement's literal value and compared.
88 of 89 preserve their value exactly. The primitive set is also value-identical to the old
file: 38 distinct literals in, 38 primitives out, **no value present in the old file is
missing from the new one and no new value was introduced.** The one exception is 0.3-002.

**Two traps the builder did not fall into**, worth recording because both were easy to miss:
`--color-surface-900` (`#0F1724`, navy) was correctly demoted to `--navy-900` and not
merged with `--text-primary` (`#0F172A`, slate — one character apart); and
`--color-text-secondary` (`#64748B`) was correctly mapped to `--text-muted`, not to the
similarly-named `--text-secondary` (`#334155`).

---

## Blocking

### 0.3-001 — The rewrite orphans every token consumer in `frontend/src`, and the only screen in the app now renders wrong

`theme.css` was rewritten; the two files that read it were not, because spec §1 forbids
touching them. The result is live on the branch.

**8 of the 11 tokens `main.jsx` reads no longer exist.** `frontend/src/main.jsx:17-27`
builds the antd theme from `getComputedStyle(document.documentElement).getPropertyValue()`,
which returns `""` for an undeclared custom property. Resolved in a real browser against
this branch's `theme.css`:

```
EMPTY     --accent-navy            ""        EMPTY     --accent-red         ""
EMPTY     --color-primary-hover    ""        resolves  --text-primary       "#0F172A"
EMPTY     --accent-blue            ""        resolves  --text-secondary     "#334155"
EMPTY     --accent-green           ""        EMPTY     --bg-content         ""
EMPTY     --accent-amber           ""        EMPTY     --bg-card            ""
                                             resolves  --font-body          "'IBM Plex Sans', …"
```

**The visible consequence, in the browser, on this commit.** `npx vite --port 5199`,
`http://localhost:5199/_dev/tokens`, viewport 1280:

```
antd primary button   background: rgb(0, 0, 0)     — was #1E40AF (BECSR navy)
antd table header     black, header text near-illegible against it
swatch cards          89 rendered · 66 resolve to an empty value
console errors        none
failed requests       none
```

Evidence: `screenshots/0.3/audit-devtokens-after-1280.png` (untracked — see 0.3-004)
against the 0.2 baseline `screenshots/0.2/tokens-1280.png`. Compare the button and the
table header directly.

`frontend/src/pages/DevTokens.jsx:6-94` enumerates all 89 old names as string literals;
the 66 blanks are the 66 whose names the rewrite retired. The 23 that still render are
exactly the retained role names (5 text, 2 status-danger, 6 chart, 2 font, 8 space).

**Root cause is the spec, not the builder's judgement.** §5.1 mandates deleting these
names; §1 out-of-scope forbids editing "anything under `frontend/src/` except
`styles/theme.css`". Those cannot both hold. Phase 1 is not completable as specified.

**What the builder owed here.** `GOVERNANCE.md §4.4` and §7 required a stop-and-report on
exactly this conflict, and §1 of the spec even provides the lesser route ("File it; do not
repair it"). Neither happened: the session report describes "Frontend sanity: npm ci &&
npm run lint && npm run build — PASS" and does not mention the orphaned consumers. See
0.3-003 for why those three commands could not have caught it.

**Remediation is the author's call, not mine. Two routes, both checked:**

1. **Amend the spec to permit updating the two consumers** to role names. I verified in the
   browser that a role-based antd theme resolves to the *same* values the old one produced:
   `colorPrimary → --action-primary = #1E40AF` · `colorPrimaryHover → --action-primary-hover
   = #1E3A8A` · `colorInfo → --status-info-marker = #3B82F6` · `colorSuccess →
   --status-ok-marker = #22C55E` · `colorWarning → --status-warn-marker = #F59E0B` ·
   `colorError → --status-danger-marker = #EF4444` · `colorText → --text-primary = #0F172A`
   · `colorTextSecondary → --text-secondary = #334155` · `colorBgLayout → --surface-page =
   #F1F5F9` · `colorBgContainer → --surface-card = #FFFFFF` · `fontFamily → --font-body`.
   All eleven resolve; none changes value. `DevTokens.jsx` would list the 113 declared
   tokens in two labelled groups — I confirmed **all 113 resolve, unresolved: NONE** — which
   also makes that page a working proof of the tier rule rather than a flat swatch dump.
2. **A back-compatibility alias block in `theme.css`.** Do not take this route. It
   reintroduces multiple names per value, which is the whole of finding 0.2-003.

---

## Functional

### 0.3-002 — `--color-neutral` changed value under a row labelled "status rename"

```
old   --color-neutral: var(--text-muted)          → #64748B   (slate-500)
new   --status-neutral-marker: var(--slate-400)   → #94A3B8   (slate-400)
```

The other four rows in that group preserve their values exactly (`--color-positive`,
`--negative`, `--warning`, `--info` → the ok / danger / warn / info markers, all at the
`-500` weight). Neutral is the only one that moved, and the note reads "status rename",
which asserts no change.

It is also internally inconsistent with the scale the builder built: every other status
marker is a `-500` primitive; neutral alone is `-400`. Whichever way it is resolved —
repoint the marker to `--slate-500`, or keep `--slate-400` and change the note to record
the deliberate shift — the deprecation table must not claim a rename when the value moved.

This is the same failure class as 0.2-002: mechanical re-pointing across two token systems
without a semantic-relationship pass.

---

## Report

### 0.3-003 — "npm ci && lint && build PASS" is a proxy signal for a claim it cannot support

`QUALITY_PROTOCOL.md §1` names this error class explicitly and cites the four false
"pushed" claims of 2026-07-26: *"an action is not done because the command exited 0. Verify
the resulting state, not the exit code."* All three commands pass on this branch — I re-ran
them — and all three are blind to 0.3-001, because a missing CSS custom property is not a
syntax error, not a lint error, and not a build error. It resolves to `""` at runtime and
antd renders black. Zero console errors, zero failed requests. Only opening the page could
have caught it, and `GOVERNANCE.md §4.2` requires exactly that.

Push claim itself verified true: `git ls-remote` → `76d5e3ac…`, identical to local.

### 0.3-004 — 0.3 screenshots cannot be committed; `.gitignore` blocks them and `.gitignore` is out of scope

```
$ git check-ignore -v screenshots/0.3/situation-1440.png
.gitignore:16:screenshots/**/*.png	screenshots/0.3/situation-1440.png
```

0.2 needed an explicit `!screenshots/0.2/*.png` negation (`.gitignore:17`). 0.3 has none,
so the nine screenshots the DoD requires will be silently ignored by `git add`. Adding the
negation touches `.gitignore`, which the DoD's own "no files touched outside" row does not
permit — and that row's permitted list (`theme.css`, `mockups/`, `CONTRACTS.md`, `dod.md`)
omits `screenshots/` altogether while another row in the same table demands nine files
there. `playthrough.md` F1 does allow `screenshots/0.3/*`. Three documents, three
different answers.

This already bit this audit: my own evidence screenshot sits untracked at
`screenshots/0.3/audit-devtokens-after-1280.png`. Resolve before Phase 2 or the visual
module ships with no visual evidence in the repository.

### 0.3-005 — CONTRACTS.md entry omits the marker every other entry carries

The entry matches spec §5.2's text verbatim, so this is a spec-inherited nit rather than a
build error. But all eight existing entries are tagged **PROSPECTIVE**, the file states that
advance-declared contracts carry that tag and are converted "as code lands", and the header
`**Last updated:** 2026-07-26 (seeded at Phase 0, before implementation)` was not touched
even though this entry now has a real producer on disk. A7 asks for "the file's existing
format". Either tag it PROSPECTIVE (consumers do not exist yet) or bump the header to
record that one contract now has a live producer.

### 0.3-006 — Part A cannot catch 0.3-001; the checklist needs a consumer check before 0.4b

Every one of A1–A8 passes on a branch where the app renders wrong. The checklist verifies
the token file's internal structure and never asks whether anything that reads it still
works. Add a Part A row before 0.4b: *load the app in a browser and assert every token name
referenced anywhere in `frontend/src` resolves to a non-empty value.* Cheap, scriptable,
and it is the check that would have turned this pass green.

### 0.3-007 — `design/06-plan-index.md` disclosed as untracked, now absent

The builder correctly reported it as outside the touch list and left it uncommitted — good
behaviour, and it is properly absent from the diff. It is now also absent from the working
tree (`git status --porcelain` clean, `ls design/` shows 01–05 only). Whatever it was, it
needs a disposition rather than a silent disappearance.

---

## Not defects in this build — spec amendments required before Phase 2 starts

Filed here so they are not mis-assigned to the builder. Each was verified against the
repository in this session. If Phase 2 begins without these resolved, the builder will hit
them one at a time and the mockups will be built against contradictory instructions.

| # | Issue | Proof |
|---|---|---|
| S-1 | **I1 + I5 + I7 are jointly unsatisfiable.** A mockup must use role tokens (I1), must not declare tokens outside `theme.css` (I7), and must contain no `<link>`/`src=`/`@import` (I5). Relative link → I5 hit; inline `<style>:root{…}` → I7 hit; raw hex → I1 hit. No compliant route exists. The playthrough's own setup ("open from the filesystem… disconnect the network") implies a relative local link is intended, i.e. I5's *check* contradicts its *intent* | spec.md:349, 353, 355 vs playthrough.md:18-21 |
| S-2 | **The Balanced Scorecard is required by §5.3 and forbidden by §2/D3.** Mockup A "must show" a four-perspective BSC summary; §2 and D3 make any metric with no home in `design/02-traceability-matrix.md` a finding — and the matrix has no BSC row (§E), while `design/03` leaves the BSC structure as future work. The four values are also absent from §5.4's fixed data | spec.md:240, 81, 84; 02-traceability-matrix.md §E; 03-scoring-frame-options.md |
| S-3 | **Three required states have no numbers.** §5.7 demands Situation *round-1-empty* and *locked*, and Applications *wizard step 3* (the item purchased has no name and no price anywhere in the spec), while §5.4 fixes the figures and D1 makes an invented number a finding | spec.md:337-339 vs 265-283, playthrough.md:82 |
| S-4 | **I6 cannot be satisfied.** §5.6 lists ~30 strings; the screens additionally need five sidebar Decisions labels and four badge labels (O1), the four BSC perspective names, `Cloud`/`On-Premises` panel titles, five slot labels, three deployment-mode names, the value-chain activity names, O2's state-section labels, and §5.5's legend text — none spec'd, while §5.6 says "nothing else may appear as visible text" | spec.md:330, 354 vs 294-328 |
| S-5 | **I3 contradicts I4.** I3 greps `casepack` in `mockups/*.html` expecting zero hits; I4's grep exempts `data-casepack`, presupposing such attributes exist | spec.md:351 vs 352 |
| S-6 | **Copy contradicts data.** §5.4 has on-prem compute at *100% used* and §5.6's capacity warning is "No headroom. The next application will not fit." — but the on-prem wizard warning is "This uses all remaining platform capacity", which asserts remaining capacity. B7 and B11 ask a naïve reader to resolve exactly this | spec.md:281, 314, 326 |
| S-7 | **O1's badge vocabulary has no student-facing labels.** `configured` / `partial` / `error` / `empty` is engine vocabulary; "error" as a student-visible badge also runs into `GOVERNANCE.md §4.9` | spec.md:117 |
| S-8 | **Missing roles for Phase 2.** No overlay/scrim role for the wizard, none for §5.5's dotted-underline annotation. §5.1 calls the role list "the contract" and adding one requires a CONTRACTS entry *and* a spec change — i.e. another mandatory stop | spec.md:159-160 |
| S-9 | **I1's grep is a false negative and a false positive.** It hardcodes `slate\|navy\|blue\|green\|amber\|red\|teal\|purple`; the builder happened to choose those stems, but the spec never fixed the naming scheme, so `--gray-100` or `--ink-900` would pass silently. And `#[0-9a-fA-F]{3,8}` matches `href="#add-to-plan"` — confirmed | spec.md:349 |
| S-10 | §5.3 says the integration line shows a **monthly** cost; §5.4 and §5.6 say "$3,100 **per round**". Round ≠ month is undefined anywhere | spec.md:254 vs 283, 316 |
| S-11 | I2 and I6 ship prose assertions rather than commands, against `SPEC_PROTOCOL.md §4` ("every verify step is executable as written"). I scripted I2 for this pass; the next auditor should not have to re-derive it | spec.md:350, 354 |
| S-12 | Signal codes `ORD-CAP-01` / `DATA-INT-02` / `EOL-POS-03` are mandated visible by §5.4 but unlisted in §5.6, and "EOL" is IT vocabulary of the kind C4 bans (RTO/RPO). §5.4's own "10 SPOFs" shorthand is an I3 hit if copied through to a mockup | spec.md:275-277, 283, 351 |
| S-13 | **Module numbering conflict.** `handoffs/README.md`'s Phase 0 table puts the design-system library at 0.3 and ten mockups at 0.4; this spec puts the library at 0.6 and splits 0.4 into a/b. `GOVERNANCE.md §7` makes the disagreement itself a finding | handoffs/README.md:157-160 vs spec.md:5, 58 |
| S-14 | **IBM Plex is not installed on this machine** — `fc-match "IBM Plex Sans"` → `NotoSans-Regular.ttf`, and no Plex files exist on disk. I5 permits a data-URI font but does not require one; without it every reviewer sees a substitute face, defeating the "reviewable with no toolchain" rationale of §3 decision 2. Available as the `fonts-ibm-plex` package if the decision is to install rather than embed | verified in session |

---

## Result

```
Run date:   2026-07-26        Auditor:  Claude (fresh context, not the spec author)
Part A:     PASS  (A1–A8, all re-run independently)
Part F:     PASS  (diff limited to CONTRACTS.md, theme.css, dod.md — mockups correctly
                   not started; F2 "building ahead" clean)
Parts B–E:  not exercisable — no mockups exist at the Phase 1 gate
Findings:   1 Blocking · 1 Functional · 5 Report · 14 spec amendments carried
Verdict:    FAIL at the Phase 1 gate  (0.3-001)
```

**On the verdict and who fixes it.** The token map itself is good work: 75 roles exactly as
specified, one-hop resolution, 89/89 deprecation rows, value-identical primitive set, two
near-miss traps avoided, the phase gate honoured, and the untracked file and the dependency
position both disclosed rather than buried. FAIL attaches to the *state of the branch*, not
to the quality of the map.

Per `handoffs/README.md` "Who fixes findings" — by cause, not severity — 0.3-001's cause is
a spec gap (§1 and §5.1 cannot both hold), not a compromised mental model. That points to
**the author amending the spec, then the same builder applying the fix**, not a fresh
builder. 0.3-002 and 0.3-005 are mechanical. The corrected branch returns to audit before
merge, and not to this context.

**Phase 2 should not start** until S-1 through S-4 are resolved. S-1 blocks the first line
of the first mockup; S-2 and S-3 make a correct Mockup A impossible to build; S-4 makes any
honest build fail I6.

---

# Amendment — rework audit, 2026-07-26

**Audited commit:** `559db6106360b159a7f467d6847090e9e91dbd85` · **Spec:** v2 (`bb39c5b`)
**Verdict: Phase 1 gate PASS with findings.** The blocking finding is closed. Three new
non-blocking findings. Phases 2–5 remain unbuilt, so the module stays open.

**Method.** Audited a **clean detached worktree** at `559db61`, not the builder's working
tree — an earlier probe of that tree read files mid-edit and produced two false failures,
which is why nothing was filed from it. All checks re-derived from spec **v2**, not v1.
The checker is a single script (15 checks, 13 passing) so that nothing here depends on my
judgement being trusted; it can be re-run by anyone against any commit.

**Independence caveat, restated and partly cured.** I proposed the eleven-token antd remap
in 0.3-001. The author has since adopted it into spec v2 §5.2 independently, so the
mapping is no longer merely my suggestion — and I verified the implementation matches v2's
table exactly and resolves to the pre-rework values. **What remains an open judgement, not
a verified fact:** whether `colorSuccess` / `colorWarning` / `colorError` should read the
`--status-*-marker` roles rather than `--accent-*`. The author and I now agree on it, which
is exactly the condition under which neither of us is a good check. Worth thirty seconds
from a third party at 0.6, when real components consume it.

## Closed

| Finding | Evidence, re-run in the clean worktree |
|---|---|
| **0.3-001** Blocking | `main.jsx` reads 11 names, **0 missing, 0 primitive refs**. All 11 antd keys match v2 §5.2 **exactly** (11/11). antd primary button `rgb(30, 64, 175)` — the house navy, was `rgb(0,0,0)`. Table header `rgb(250, 250, 250)` with text `rgb(15, 23, 42)` — legible, was black. `/_dev/tokens` renders **115 cards, 0 blank** — was 66 blank of 89. Zero console errors, zero failed requests. `border-radius: 0px` holds. My own capture, not the builder's: scratchpad `audit-rework-1280.png`; the builder's tracked proof at `screenshots/0.3/devtokens-rework-1280.png` agrees |
| — *and the guard* | v2 §5.2 additionally required `token()` to throw rather than return `""`. Implemented at `main.jsx:10-16`. **The builder's session report did not mention it**; it is there and it is correct. This is the durable fix — the next deleted token fails loudly instead of rendering black |
| **0.3-002** Functional | `--status-neutral-marker: var(--p-slate-500)` → `#64748B`, the original `--color-neutral` value. The deprecation note was also corrected to *"status rename, value preserved at `#64748B`"* rather than left claiming a bare rename. That correction is what makes 0.3-009 below a finding rather than a matter of taste — the right standard now exists two rows away in the same table |
| **0.3-005** Report | `CONTRACTS.md`: heading now `Design tokens — two-tier — PROSPECTIVE`, header bumped to *"(0.3 design-token contract added)"*, primitive examples re-prefixed `--p-` |
| **0.3-004** Report | Closed by the author: `.gitignore:18` `!screenshots/0.3/*.png`. Verified the screenshot is genuinely **tracked in the remote tree** (`git ls-tree -r origin/…`), not merely `git add`-ed locally |
| **0.3-006** Report | Closed by the author: v2 adds **I8** — "antd reads only live role names" — the consumer check whose absence let a green Part A sit on a broken app. It is the check that would have caught 0.3-001, and it now passes |
| **0.3-007** Report | `design/06-plan-index.md` is tracked at this commit |
| **S-1 … S-14** | Addressed in spec v2, which states it fixes all 17. Spot-verified: **S-1** §4 names one compliant route and decision 2 permits the relative `<link>` · **S-2** BSC figures fixed in §5.4 and `design/02-traceability-matrix.md:100` now carries a Balanced Scorecard row · **S-3** §5.4 adds the round-1 state block · **S-5** I4 no longer greps `casepack` · **S-9** `--p-` prefix mandated, hex check now declaration-context · **S-10** §5.3 says per-round · **S-12** signal codes replaced with plain English ("Order system near capacity") and "10 single points of failure" spelled out · **S-13** `handoffs/README.md` renumbered to 0.3 / 0.4b / 0.6. Full verification belongs to the Phase 2 audit, against actual mockups |

**S-6 partially addressed — reasserted for Phase 2.** The on-prem wizard warning changed
from "This uses" to "This **would** use all remaining platform capacity" (§5.6:344) while
§5.4:251 still holds on-prem compute at **100%** and §5.6:328 still says "No headroom." A
subjunctive does not resolve the arithmetic: with no headroom there is no remaining capacity
to use. Checklist items B7 and B11 ask a naïve reader to reconcile exactly this. Not a
Phase 1 defect; it is on the Phase 2 auditor's list.

## New findings

### Report

**0.3-008 — DoD row 1 marks pre-flight rows 1–8 PASS; row 6 verifiably fails.**

The row's evidence covers the v2 version gate and row 3 only. Row 6 is not satisfied and
its instruction was not followed:

```
$ fc-match "IBM Plex Sans"        NotoSans-Regular.ttf: "Noto Sans" "Regular"
$ fc-list | grep -ci plex         0
$ apt-cache policy fonts-ibm-plex Installed: (none)   Candidate: 6.1.1-1
```

Spec v2 row 6 reads: *"an IBMPlex font. If Noto → `sudo apt install fonts-ibm-plex`,
re-check, **report**."* The install did not happen and neither did the report. Per the
auditor's standing instruction, a row marked PASS without evidence is a FAIL. Phase 1's
deliverable does not depend on the font, which is why this is Report and not Functional —
but see 0.3-010, because Phase 2's entire acceptance is how a screen reads, and
`GOVERNANCE.md §4.3` names IBM Plex Sans specifically.

**0.3-009 — the two font-stack changes are recorded as "role retained"; both values changed.**

```
--font-body   'IBM Plex Sans', system-ui, …        →  'IBM Plex Sans', 'Arial', system-ui, …
--font-mono   'IBM Plex Mono', 'Source Code Pro'   →  'IBM Plex Mono', 'Courier New'
dod.md:118-119  note: "role retained"
```

The change itself is authorized — v2 decision 4 requires the stack to declare a visible
fallback. The record is what is wrong: "role retained" asserts an unchanged definition.
Identical class to 0.3-002, corrected in the same table twenty rows earlier.

**0.3-011 — `IBM Plex Mono` is declared but never delivered. Found by challenging the
severity of 0.3-008, not by the original pass.**

```
$ grep -rn "@font-face" frontend/src frontend/index.html      NONE
$ git ls-files | grep -iE "\.(woff2?|ttf|otf)$"               NONE
$ grep -niE "fontsource|@ibm/plex" frontend/package.json      NONE
frontend/index.html:7   …css2?family=IBM+Plex+Sans:wght@400;500;600;700    ← Sans only
frontend/src/styles/theme.css:32   --p-font-mono: 'IBM Plex Mono', 'Courier New', monospace
```

Plex **Sans** is delivered by CDN, so the body typeface is correct in production — a point
this audit initially assumed the other way and verified only when pushed on. Plex **Mono**
is named by a role and shipped by nothing, so every monospace surface renders Courier New
(or Liberation Mono on Linux). Today the only consumer is `.swatch-copy span` on
`/_dev/tokens`, which ships to no student; it becomes visible the first time a figure, code
sample, or data table takes the mono role.

**Not a 0.3 defect** — the role and the stack both predate this module, and `index.html`
was authored at 0.2. Per `handoffs/README.md` ("finding belongs to a later module → carry it
into that module's spec") this belongs to **0.6**, where the component library decides font
delivery. Two candidate routes, neither verified here: add `IBM+Plex+Mono` to the existing
CDN request, or self-host both faces and drop the third-party runtime dependency — which for
a university cohort also removes a Google Fonts call from every student's browser.

### UX — Phase 2 gate, not a Phase 1 defect

**0.3-010 — `'Arial'` does not achieve what v2 decision 4 asks of it.**

Decision 4 wants absence to "degrade **noticeably** rather than silently." Measured on this
machine:

```
$ fc-match "Arial"          LiberationSans-Regular.ttf: "Liberation Sans"
$ fc-match "Courier New"    LiberationMono-Regular.ttf:  "Liberation Mono"
$ fc-match "IBM Plex Sans"  NotoSans-Regular.ttf         ← nothing is installed
```

Liberation Sans is a metric-compatible neutral grotesque; so is IBM Plex Sans. With Plex
absent, a reviewer sees a competent-looking sans and receives **no signal at all** that the
house typeface is missing — the exact silent degradation decision 4 was written against.
Body text resolved in-browser to `"IBM Plex Sans", Arial, system-ui, …` and rendered as
Noto. Mono fares better: Liberation Mono reads as a typewriter face and does announce
itself.

The effective safeguard is not a fallback entry, it is pre-flight row 6 —
`sudo apt install fonts-ibm-plex`, then re-check. If a fallback is also wanted as a
belt-and-braces signal, it should be a face nobody mistakes for Plex, and the reasoning
should be recorded rather than inferred from a stack edit.

## Result

```
Run date:   2026-07-26        Auditor:  same auditor as the Phase 1 pass, fresh worktree
Checks:     15 re-derived from spec v2 · 13 PASS · 2 FAIL (0.3-009 VAL, 0.3-008 PF6)
Part A:     PASS — 77/77 roles exactly once, one-hop resolution, 38 --p- primitives
Part F:     PASS — diff limited to theme.css, main.jsx, DevTokens.jsx, CONTRACTS.md,
                   dod.md, one screenshot. Phase 2 correctly not started
Findings:   0 Blocking · 0 Functional · 1 UX · 0 Data · 3 Report
Verdict:    Phase 1 gate PASS with findings. Module open — Phases 2–5 unbuilt
```

**0.3-012 — `design/06-plan-index.md` contradicts itself on Phase 0 numbering.**

```
06-plan-index.md:18-33   correction block: "Phase 0 keeps its historical IDs"
                         → 0.3 · 0.4b · 0.6
06-plan-index.md:41-46   Phase 0 table, ten lines below
                         → 0.3 · 0.4 · 0.5, annotated "(was 0.3)" etc.
handoffs/README.md:159-162            follows the prose  → 0.3 · 0.4b · 0.6
handoffs/0.3-mockup-pilot/spec.md:5  follows the prose  → blocks 0.4b, 0.6
```

The file calls itself "the authoritative list", so this is the one place the ambiguity is
most expensive. Two of three other documents follow the prose reading. `GOVERNANCE.md §7`
makes the disagreement itself the finding, so this audit did not pick a side — the carried
findings written into that file name their destination packet **by function** rather than by
ID. **Owner: author. Closes when:** one of the two readings is deleted. Cheap, and it gets
cheaper the sooner it happens — every new packet referencing either scheme raises the cost.

### Which gate each finding must clear

Added after the verdict was challenged on the right grounds: *"if we cannot launch without
addressing them, they are blocking."* Severity under `QUALITY_PROTOCOL.md §4` answers **why
this happened** — Blocking is stipulated there as *"the workflow cannot complete"* — and it
carries a routing consequence, since `handoffs/README.md` sends a FAIL verdict to a **fresh
builder**. The shipping question is answered by the **gates**, and reporting severity without
naming the gate left that to inference. It should not have.

| Finding | Stops a cohort? | Gate it must clear | Cost |
|---|---|---|---|
| 0.3-008 pre-flight row 6 false PASS | No — production serves Plex Sans by CDN | **Phase 2**, and Phase 0's "mockups approved" gate: a reviewer on an un-provisioned machine sees Noto | one `apt install`, then re-report the row |
| 0.3-009 "role retained" on two changed rows | No — students never read a build report | **Merge, and 0.6.** The deprecation table is the only map from the 89 old names to the new ones, and 0.6 ports globalstrat components against it | a two-word edit |
| 0.3-010 `'Arial'` is not a visible fallback | No | **Phase 2 mockup review** | one decision |
| 0.3-011 Plex Mono undelivered | **Yes, eventually** — not today, as only a dev page consumes the mono role | The component-library packet, and hard-stops at **Phase 7 pilot readiness**. **Carried into `design/06-plan-index.md` §Phase 0 → "Carried findings"** so the packet's author consumes it; it does not rely on anyone re-reading this file | one CDN parameter, or self-host both faces |
| 0.3-012 plan-index numbering self-contradiction | No | **Author, now.** Cost rises with every packet that cites either scheme | delete one of the two readings |

Nothing here stops the Phase 1 gate and nothing stops a cohort today. **0.3-011 must not
reach a cohort.** Clearing all four before Phase 2 is a defensible call and cheap — but it is
a scheduling decision, not a severity one, and it should not be recorded as FAIL, because
that word reassigns the work to a builder with no context.

**Who fixes.** All three are mechanical or documentary, so per `handoffs/README.md` — by
cause, not severity — the **same builder** takes them. None blocks the Phase 1 gate. 0.3-008
and 0.3-010 should be cleared before Phase 2 begins, since Phase 2's acceptance is visual
and currently no reviewer would see the house typeface.

**What the builder did well, recorded because the protocol only ever records failures.**
The throw-on-empty guard converts this whole failure class from silent to loud, and it went
in without being flagged in the report. `0.3-002` was fixed *and* its note corrected — the
harder half, and the half most builds skip. The phase gate was honoured under pressure to
continue. The push claim was verified by ref *and* by tree.

**Standing constraint for Phase 2.** I remain disqualified from Part B of the playthrough:
I have now read §5.6's copy list and §5.4's figures three times. Part B requires a reader
who has never seen the spec, and that reader must be someone else.
